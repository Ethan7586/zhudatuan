# AU-324｜Compatibility 成员运营数据库契约

该事务测试验证邀请固定指向 employee/storefront 身份、管理员建档不能产生 Owner 且必须首次改密、首次改密成功后清除强制状态、普通资料更新可用、Owner profile 不可修改，以及导入报告和运营中心投影可读取。全程 rollback，不保留测试数据。

该测试没有构造同租户同企业、不同商城的 mall-scoped 操作者与目标 membership；因此不能覆盖 AU-317 的 F-0237 跨商城目标范围缺口，也不能作为该问题的反证。未找到正式执行入口，按审计纪律未运行。

未发现新增 P0/P1 或删除候选；F-0237 保持 P2 且待独立复核。该文件为 G0（真实数据库契约测试）。
