# AU-411｜微信支付查询队列

## 范围与方法

- 主审文件：`02_platform_pingtai/database/supabase/migrations/20260820127600_wechat_payment_query_queue.sql`。
- 交叉核对：后续查询结果契约、支付恢复契约和 Commerce 恢复队列实现。
- 本批为静态调用链与迁移语义审阅；未执行数据库重放、构建或线上操作。

## 运行结论

迁移为微信付款尝试建立查询/关单领取、租约、退避、死信及审计机制。所有领取、锁定和失败回退路径遵守订单—支付—尝试锁顺序；过期租约达到上限会记录死信，合法任务以 `SKIP LOCKED` 领取。内部锁函数不开放，领取与失败入口仅允许 service role。

查询结果的后续迁移及契约测试继续依赖这一租约语义。然而固定基线没有找到 canonical 查询 RPC 的仓内运行调用；Commerce 恢复队列使用独立的 `access.purchase_enqueue_payment_query` 模型。无法排除仓外作业或已部署 service-role 调度。

## 审计结论

- DC-0065 / G1：不得删除；先核验生产支付查询消费者、调用日志、待处理尝试和微信对账恢复路径。
- 本批未新增 P0、P1、P2 或 P3；未运行验证均已明确保留为未验证状态。
