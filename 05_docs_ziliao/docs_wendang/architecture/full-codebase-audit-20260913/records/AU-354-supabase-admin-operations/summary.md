# AU-354｜Supabase 管理运营真实写路径

`20260810130000_admin_operations_real_write_path.sql` 建立管理端目录、订单/售后范围读模型与商品/订单资源范围反查；商品发布状态和订单发货命令按商城+请求键取得事务咨询锁、校验请求哈希、锁定目标资源、写入幂等响应并保存 membership 与授权证据审计。所有 RPC 仅授予 `service_role`。

当前 Commerce API 的后台目录、概览、订单读取、商品上下架与订单发货均实际调用这些函数。资源地址型写入在 API 先加载数据库目标范围再做授权决定，数据库命令再以 tenant/enterprise/mall 约束目标，形成双层范围事实。该迁移归 G0。

未发现新增 P0–P3 或删除候选。未运行管理写入、发货、迁移或数据库查询。
