# PRODUCT-000A 本地预览唯一版本说明

## 1. 唯一源码身份

- 工作区：`/Users/Ethan/.codex/worktrees/product-000a-baseline-isolation-20260901`
- 分支：`codex/product-000a-baseline-isolation`
- 批准基线：`0db535e2475c017dda8bb055344a240f6e2ae1b9`
- 包管理事实源：npm + 根 `package-lock.json`（lockfileVersion 3）
- 禁止进入本线：`pnpm-lock.yaml`、候选脏树业务文件、迁移 ledger 改写、Preview/Mock 数据。

当前页面 Footer 显示 `Build <commit/build> · DB <database>`；完整 commit、分支、API、数据库和 dirty 状态在该元素的 `title` 中。标识由构建时 Git 状态与显式 `VITE_DATABASE_ID` 组成，不根据页面数据猜测。

## 2. 本批精确文件边界

PRODUCT-000A 只允许修改：

1. `apps/console/vite.config.ts`
2. `apps/console/src/shared/config/BuildInfo.ts`
3. `apps/console/src/shared/config/BuildInfo.test.ts`
4. `apps/console/src/shell/ScopeShell.tsx`
5. `apps/console/src/shell/ScopeShell.test.tsx`
6. `scripts/check/local-preview-runtime.mjs`
7. `scripts/check/local-preview-database.mjs`
8. `docs/本地预览唯一版本说明.md`

不修改 Contract、SDK、公共 CSS、Design、业务 Handler、已登记迁移、RLS/Grant、生产配置或任何数据库事实。

## 3. 206 项候选差异冻结与归属

原候选只读路径：`/Users/Ethan/.codex/worktrees/pluggable-connectors-backend-20260901`。

- 状态项：206
- 已跟踪差异：149 files，`+2569/-1014`
- 状态清单 SHA-256：`36b432952313caeed412d590995bc21212281c1115b4dcd270d4ab4e556d8c94`
- 原候选不清理、不 reset、不作为集成分支。

逐项归属由 `local-preview-runtime.mjs --candidate-root ...` 生成并要求零 `UNASSIGNED`。冻结快照分组如下：

| 归属                                   |    数量 | 处置                                               |
| -------------------------------------- | ------: | -------------------------------------------------- |
| PRODUCT-000A                           |       6 | 仅重新实现可追溯与检查能力，不复制候选脚本         |
| PRODUCT-000R                           |       2 | 迁移清单和 Runner 进入独立串行修复批次             |
| PRODUCT-000R-MIGRATION                 |       1 | 整体拒收；移除 ledger 改写后重新设计为后续受管迁移 |
| PRODUCT-001                            |       4 | ProductWorkspaceProjection 串行线                  |
| PRODUCT-002 / REMOVE-PREVIEW           |      32 | 商品工作台；Preview 目录不得进入正式源             |
| PRODUCT-003                            |       2 | 商品详情                                           |
| PRODUCT-004                            |       1 | Catalog 导入落地                                   |
| PRODUCT-005 及其 Contract/SDK/集成队列 |      90 | Connection 与连接器合同；生成物必须重新生成        |
| PRODUCT-006                            |       1 | 测试连接与启停                                     |
| PRODUCT-007 及集成队列                 |      11 | 同步正确性与恢复                                   |
| PRODUCT-008 及 Contract 队列           |      22 | Owner Inbox 与供给事实                             |
| PRODUCT-013                            |      12 | 锁定 VI/响应式批次                                 |
| PRODUCT-013-APPROVAL-REQUIRED          |      13 | 候选改了公共 Design；没有 Ethan 新批准不得带入     |
| PRODUCT-014                            |       3 | 最终生成物、合同和需求收口                         |
| EXCLUDED-LOGIN                         |       2 | 登录不属于商品批次                                 |
| EXCLUDED-PAYMENT                       |       4 | Payment 不进入商品管理                             |
| **合计**                               | **206** | 每项均有归属，无未知文件                           |

## 4. 唯一预览端点

| 能力         | 唯一端点                | 必须来自                                               |
| ------------ | ----------------------- | ------------------------------------------------------ |
| Console      | `http://127.0.0.1:5274` | 本工作区 `apps/console`                                |
| Auth Web     | `http://127.0.0.1:5275` | 本工作区 `apps/auth-web`                               |
| Commerce API | `http://127.0.0.1:3211` | 本工作区真实 `ApiMain`                                 |
| PostgreSQL   | `127.0.0.1:55490/shop`  | PRODUCT-000A 独立容器；库名沿用批准迁移器要求的 `shop` |

其他任务可以在其他端口受控并行；它们不因 PRODUCT-000A 被停止或删除。是否属于本预览由“端点 + 进程 cwd + Git HEAD + API 健康响应 + 数据库身份”共同判定，不能只看端口是否在线。

## 5. 运行闸门

源码与候选归属检查：

```bash
PRODUCT_PREVIEW_CANDIDATE_STATUS_SHA256=36b432952313caeed412d590995bc21212281c1115b4dcd270d4ab4e556d8c94 \
node scripts/check/local-preview-runtime.mjs --source-only \
  --candidate-root /Users/Ethan/.codex/worktrees/pluggable-connectors-backend-20260901
```

完整运行态检查：

```bash
node scripts/check/local-preview-runtime.mjs
```

隔离数据库只读检查（URL 由本地运行环境提供，不写入文档）：

```bash
PRODUCT_PREVIEW_DATABASE_URL='<local-postgres-url>' \
PRODUCT_PREVIEW_DATABASE_NAME='shop' \
node scripts/check/local-preview-database.mjs
```

完整闸门只有在以下事实全部成立时输出 `result=PASS`：

1. 当前分支以批准基线为祖先，dirty 文件均属于 PRODUCT-000A。
2. 根目录只有 `package-lock.json`，且 lockfileVersion 为 3。
3. 本批迁移不存在对 `runtime.schemaversion` 的 INSERT/UPDATE/DELETE。
4. Console、Auth、API 进程 cwd 与 Git HEAD 一致。
5. API `/health/ready` 返回 canonical schema/contract/registry/database 状态，不接受硬编码假 API。
6. Console 的 `/api/health/ready` 与直连 API 返回相同 schema 和 contract。
7. 数据库连接为显式本地端点，实际 database 名与 `PRODUCT_PREVIEW_DATABASE_NAME` 相同，并在只读事务中完成核对。

隔离身份由独立容器、端口 `55490` 和进程来源共同确定。批准基线的 `tools/seed/src/Migrate.ts` 明确执行 `ALTER DATABASE shop`，因此本批不修改该既有工具，也不篡改已登记迁移；独立容器内部保留库名 `shop`。

## 6. 回滚

本批没有迁移、业务数据或生产状态变化。回滚只需移除新 worktree/分支；批准基线和 206 项原候选保持不动。不得使用 `reset --hard`，不得删除未归属文件。

## 7. Fresh migration 阻塞证据

隔离空库上的 `npm run local:migrate` 没有进入业务迁移执行阶段，依次暴露两个批准基线缺口：

1. `REPAIR_FILES` 漏列仓库已存在的 `20260831150000_identity_experience_application_commands.sql`；PRODUCT-000A 不修改迁移清单，该缺口进入 PRODUCT-000R。
2. 该迁移登记 marker 为 `0ff4aba32aa64206955513760655589f9d0537598f1e47b174b49ae98fd7b074`，但将 marker 归零后的文件 SHA-256 为 `09f2eb4b733cb7a968348467a64d42b0783b0b101b145e7ba137572c21aa3df0`；同时 `RegistrationMigrationRunner` 仍以 `20260831140000` 为 target。

2026-09-01 对生产 `zhudatuan-registration-postgres` 的只读查询证明 `20260831150000` 已登记，checksum 正是 `0ff4…b074`。因此：

- 不得修改该迁移或生产 ledger；
- 不得跳过 fresh-migration 守卫；
- 不得把候选 `20260901010000` 中直接 UPDATE/INSERT ledger 的做法带入；
- 只能在共享迁移串行窗口创建新的后续受管修复迁移，并同步 RegistrationMigrationRunner target/checksum。

在该依赖解决前，完整同源 API + 隔离 DB Journey 不得标记为 PASS，PRODUCT-001 不得从本分支开工。本批启动的 8543～8545 服务和 55490 临时容器已全部清理，其他任务端口未改动。

`npm run check:generated` 还有一个独立基线阻塞：批准 commit 缺少 `apps/miniapp/miniprogram/styles/tokens.wxss`，该文件只来自批准工作树原有的未跟踪 `apps/miniapp/` 目录。PRODUCT-000A 不复制、删除或吸收未归属目录；必须先由对应 Owner 将正式生成资产纳入可追溯提交，或明确修正 generator 的事实来源。
