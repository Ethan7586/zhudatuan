# RV-0041｜成员与券批量导入完成迁移独立复核

- 复核日期：2026-09-15
- 基线：`5a1ce71eebbefaa826368a9e1dc17730f9363bc4`
- 对应候选：GX-0019
- 结论：**维持 GX，禁止删除、改写或单独重放；未发现 P0。**
- 方法：重新审阅迁移、成员与券导入 API/持久化/Worker 及应用作业注册；未访问成员、券码、导入对象或生产数据库。

## 迁移责任

迁移把 member 的历史 cursor 转为整数并回填进度，给 member/voucher import job 增加受限状态、验证摘要、错误和报告对象约束；它创建仅 shopjob 可读写的 member/voucher 暂存行。券暂存行强制“有效密文+指纹+密钥版本”或“仅错误码”二选一，并以 job 内指纹唯一键防止重复明文券码被再次引入。目标断言拒绝处理数超出 cursor 的成员或券导入。

它是个人资料和券码输入的数据最小化、恢复游标和批处理幂等边界，不能视为完成后可删的历史脚本。

## 当前运行关系

应用 Job 目录正式注册 `memberimport` 和 `voucherimport`。成员创建 API 写入组织 scope 的 uploaded job 并投递初始 job；读取 API 也以 organization ID 限制 job 与错误报告。成员持久化在事务中锁定 ready/running job，并以 cursor 生成稳定续跑 job。券导入按 500 行读取仅 Worker 可见的密文暂存行、以 savepoint 隔离失败，并仅以指纹写入 card。

## 裁决与未知项

维持 GX。已静态确认当前 API、Worker、进度/续跑、敏感暂存权限及指纹去重仍依赖目标结构；未验证旧 cursor 转换、真实成员归属、券码密文/密钥、重复导入、取消/重试、报告对象、备份恢复或 schema ledger。后续变更须从届时最新主线建立独立成员/券/数据库专项；本审计分支未处理任何生产输入。
