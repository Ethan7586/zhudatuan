# AU-397｜券运营基础

`20260820110000_voucher_operations_foundation.sql` 建立储值券项目、卡池、备券申请/审批、发行批次、券余额、状态事件、核销、冲正和作废余额对账。余额变化使用行锁、版本号、幂等键和不可变事件/审计；读取函数从服务端解析的 membership 派生业务范围，不接收浏览器提供的范围 ID。

固定基线未找到应用、Worker、运维脚本或数据库契约测试调用该券资金链，因此全链归 DC-0060/G2，禁止删除。更重要的是，迁移虽定义并分配了细粒度券权限，但七个写入 RPC 只验证身份匹配与范围，不校验具体动作权限或授权证据；已记录为 F-0241/P1 候选，必须由第二位审计者重新追踪迁移 ledger、service-role 调用者和权限边界。

未发现 P0；未执行测试、数据库写入、构建或线上检查。
