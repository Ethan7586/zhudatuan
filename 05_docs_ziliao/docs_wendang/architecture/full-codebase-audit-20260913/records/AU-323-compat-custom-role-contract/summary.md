# AU-323｜Compatibility 自定义角色数据库契约

该事务测试创建自定义角色、将其分配给测试管理员、编辑权限并断言授权版本递增；随后停用角色并断言所有有效分配撤销。它还验证 Owner 角色不可复制、非 Owner 不能创建超出自身权限的角色，以及 Owner 可向目标 membership 写入商业品牌/门店范围。测试始终 rollback，不留下夹具。

覆盖重点与 AU-319 的运行实现一致：角色生命周期、权限上限、Owner 保护、角色变更授权失效和完整范围写入。未覆盖 HTTP 路由、重新验证、数据库迁移顺序或非 Owner 的所有范围组合；这些边界由关联 API 测试和 AU-319 实现取证承接。未找到该 SQL 测试的正式仓库执行入口，且审计纪律不发明命令，故未运行。

未发现新增 P0–P3 问题或删除候选；结论为 G0（真实数据库契约测试）。
