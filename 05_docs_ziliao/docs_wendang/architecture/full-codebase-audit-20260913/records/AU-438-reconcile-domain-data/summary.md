# AU-438｜领域数据对账

## 范围与方法

- 主审文件：`02_platform_pingtai/database/supabase/migrations/20260821027000_reconcile_domain_data.sql`（151 行）。
- 交叉核对：前置全域回填迁移及数据库契约脚本的固定迁移序列。
- 本批为静态迁移语义审阅；未执行数据库重放、读取生产数据、构建或测试。

## 运行结论

迁移将源/目标行数、金额差和关键业务哈希写入 runtime reconciliation evidence/hash 表，并在事务内对组织、成员、商品、库存、订单、支付、权益、券、财务、履约、通知、身份和运行时数据做多组对账。随后以 fail-closed 断言阻止任何数量/哈希差异、财务不平、重复外部对象/订单/券指纹及跨域孤儿引用进入后续迁移阶段。

它是 AU-437 历史回填的验收门禁，数据库契约脚本将二者置于固定序列。未成功通过该迁移时，后续迁移不会完成；这使其具有迁移可验证性和恢复诊断责任。

## 审计结论

- GX：全域迁移验收与历史数据责任。禁止删除、修改、单独重放或把旧 `public.*` 引用误判为运行冗余。
- 静态审阅未发现 P0–P3；真实目标环境是否通过所有对账只能由隔离迁移演练和 ledger/数据证据证明，本批保持未验证。
