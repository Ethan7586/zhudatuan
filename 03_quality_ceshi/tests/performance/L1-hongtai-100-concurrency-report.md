# L1「宏泰甄选」100 人并发全链路模拟报告

## 结论

**通过。** 正式运行 `l1-hongtai-1788737688943-d756d8d9` 在全新隔离 PostgreSQL 17.11 中按固定闸门完成 `1 → 10 → 100` 三档。100 人档所有 HTTP 阶段、短信任务、支付查询、退款任务和最终隔离断言均为 100% 成功；无 4xx/5xx、连接超时、未收敛事务冲突或 deadletter。

该结论证明当前版本在本机隔离环境中可以承接 100 名用户近同时启动并走完“注册 → 登录 → 商品 → 购物车 → 报价 → 下单 → 支付 → 售后 → 退款”链路。100 人端到端 p95 为 40.121 秒，正确性已经达标，但这不是低延迟结论；其中同一热销库存的下单 p95 为 6.282 秒，仍应作为后续容量优化基线。

本次没有部署、推送、提交、修改生产配置或触碰生产数据。

## 正式运行环境

| 项目 | 实际值 |
|---|---|
| 运行时间 | 2026-09-07 07:34（Asia/Shanghai） |
| 代码基线 | `8f962fe0f3019c079c174223940be86eec0c36d5` 加当前未提交修复 |
| Node.js | v22.22.3 |
| PostgreSQL | 17.11；全新临时容器，运行后已删除 |
| 数据库结构 | 243 条迁移全部回放通过（historical 94、repair 149） |
| HTTP | 内存中的真实 `HttpApp` Request/Response；真实路由、契约、Cookie、CSRF、授权管线 |
| 数据库角色 | Identity API/Jobs、Web API、Purchase API、综合 App、Shop Jobs 分离角色 |
| 仅有替身 | 短信 debug 投递、微信 JSAPI 预支付/查询/退款、测试密文 envelope 适配器 |
| 测试商品 | 同一 L1 商品；单价 25.90 元，每单 2 件，应付 51.80 元 |

运营审批使用独立测试运营身份直接调用实际审批用例；消费者侧全部请求均使用当次注册与登录产生的真实会话。

## 三档结果

| 并发用户 | 启动跨度 | 端到端 p95 | 最大耗时 | 密码哈希 p95 | DB 连接峰值 | RSS 峰值 | 结果 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | 0 ms | 5.870 s | 5.870 s | 68.133 ms | 6 | 192.7 MiB | 通过 |
| 10 | 0.440 ms | 9.283 s | 9.283 s | 259.475 ms | 41 | 291.5 MiB | 通过 |
| 100 | 5.661 ms | 40.121 s | 40.123 s | 2.140 s | 94 | 293.4 MiB | 通过 |

100 人档事件循环最大延迟 6.456 ms，CPU 折合单核 34.853%。密码哈希 p95 随并发升高到 2.140 秒，反映本机同时执行 scrypt 时的 CPU 竞争，已与注册 HTTP、短信队列分别计量。

## 100 人档阶段结果

| 阶段 | 请求 | 状态 | p95 | 阶段吞吐 |
|---|---:|---|---:|---:|
| 注册挑战 | 100 | 202 × 100 | 695.835 ms | 135.576 req/s |
| 注册 | 100 | 201 × 100 | 3.086 s | 31.308 req/s |
| 登录挑战 | 100 | 202 × 100 | 631.753 ms | 149.766 req/s |
| OTP 登录 | 100 | 201 × 100 | 870.976 ms | 108.669 req/s |
| 票据交换 | 100 | 200 × 100 | 526.898 ms | 179.805 req/s |
| 商品目录 | 100 | 200 × 100 | 1.215 s | 82.027 req/s |
| 加购 | 100 | 200 × 100 | 2.106 s | 44.252 req/s |
| 数量更新 | 100 | 200 × 100 | 2.118 s | 42.352 req/s |
| 购物车读取 | 100 | 200 × 100 | 996.585 ms | 99.959 req/s |
| 结账报价 | 100 | 201 × 100 | 3.094 s | 32.088 req/s |
| 订单首次创建 | 100 | 201 × 100 | 6.282 s | 15.066 req/s |
| 订单同键重放 | 100 | 201 × 100 | 1.225 s | 80.257 req/s |
| 支付意图 | 100 | 201 × 100 | 1.749 s | 56.972 req/s |
| 已支付订单读取 | 100 | 200 × 100 | 1.211 s | 82.308 req/s |
| 售后首次申请 | 100 | 201 × 100 | 1.860 s | 49.805 req/s |
| 售后同键重放 | 100 | 201 × 100 | 978.338 ms | 101.595 req/s |
| 售后审批 | 100 | 200 × 100 | 1.827 s | 50.999 req/s |
| 退款后最终读取 | 100 | 200 × 100 | 1.007 s | 99.042 req/s |

首次请求与幂等重放分成两个各 100 请求的波次，所以任一阶段的瞬时业务请求数不超过声明的 100 并发用户；两轮响应逐用户做规范化精确比对。

## 队列与隔离证据

100 人档的两批短信任务均为 `completed × 100`，排空分别耗时 114.359 ms 与 66.673 ms；支付查询为 `completed × 100`，排空 6.083 s；退款为 `completed × 100`，排空 970.530 ms。全库 deadletter 为 0。

三档累计形成订单、售后与成功退款各 111 条。100 人档验证结果：

- cart、cart item、order、aftersale、payment、refund、session 均严格为每用户 1 条；
- 跨用户购物车项目为 0，错误订单 owner 为 0；
- 100 条售后全部完成，100 条退款全部成功；
- 订单与售后同键重放均未产生重复聚合；
- 跨商城测试商品始终不可见。

`fulfillment` 与 `orderexpiry` 各保留 111 个 queued 任务，因为履约发货和 30 分钟未支付关单不在本链路范围；`auditarchive`、`extensionhealth` 各有 1 个初始化任务。这些均属于执行前定义的预期残留。

## 本轮修复

1. 为退款任务、退款完成比较和 `payment.refunded` outbox 的 PostgreSQL 动态参数补齐显式类型。
2. 让退款任务重试接受已进入 `processing` 的售后状态，避免成功转态后的任务重试被误判冲突。
3. 命令事务在开启 SERIALIZABLE 快照前按稳定顺序取得审计/共享库存锁；同进程同键请求先在连接池外排队，数据库咨询锁继续承担跨进程协调。
4. 注册角色与作用域的 `effective_at` 使用事务时间戳，避免容器时钟微调造成边界倒序。
5. 修正压测口径：首次请求与幂等重放不再同时叠加成 200 个请求；测试商品、绑定与发布的生效时间固定在明确过去，避免夹具卡在“刚刚生效”的时钟边界。

## 验证记录

- 定向回归：5 个测试文件、30 个测试全部通过。
- Commerce TypeScript 检查：通过。
- 压测脚本独立 strict TypeScript 检查：通过。
- PostgreSQL 17 全量迁移回放：通过。
- 正式隔离运行：退出码 0；`ALL_1_10_100_COHORTS_PASSED`。

复验入口：

```bash
node 04_tools/scripts/audit/l1-hongtai-concurrency.pg17-fixture.mjs
```

机器证据：`03_quality_ceshi/tests/performance/evidence/l1-hongtai-concurrency-latest.json`。
