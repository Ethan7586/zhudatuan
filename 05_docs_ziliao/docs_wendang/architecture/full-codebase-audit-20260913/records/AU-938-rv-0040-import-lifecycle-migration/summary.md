# RV-0040｜批量导入进度、暂存行与报告迁移独立复核

- 复核日期：2026-09-15
- 基线：`5a1ce71eebbefaa826368a9e1dc17730f9363bc4`
- 对应候选：GX-0018
- 结论：**维持 GX，禁止删除、改写或单独重放；未发现 P0。**
- 方法：重新审阅迁移、目录/库存/成员/券导入 Worker、进度/暂存访问路径和作业注册；未访问导入文件、对象存储或生产数据。

## 迁移责任

迁移回填目录导入的 cursor/验证摘要，增加受限状态机、进度、错误和报告对象完整性约束，并将错误按 scope 化；它创建库存导入 job/row/error，限制应用身份访问暂存行而允许 job 身份处理。目标断言会拒绝目录导入已处理数超过 cursor 的不一致状态。

这定义了大批量输入的租户隔离、恢复游标、错误报告与原始数据暴露边界，不能从同类 Worker 存在推断迁移已可删除。

## 当前运行关系

应用 Job 目录正式注册 `memberimport`、`catalogimport`、`inventoryimport` 和 `voucherimport`。目录导入要求 job scope；成员和券导入在事务中锁住 ready/running job、按 500 行读取暂存数据，并以 savepoint 隔离单行失败后更新成功/失败和 cursor。运营确认目录导入时只在当前 scope 将 ready job 转 running 并投递 runtime job；券暂存额外写入密文、指纹及密钥版本。

## 裁决与未知项

维持 GX。已静态确认当前作业、游标、暂存授权和失败隔离仍依赖目标结构；未验证旧 job 回填、跨 scope 拒绝、重复对象、取消/重试、报告对象、生产数据恢复或 schema ledger。后续变更须从届时最新主线建立独立导入/数据库专项；本审计分支未读取导入对象或执行作业。
