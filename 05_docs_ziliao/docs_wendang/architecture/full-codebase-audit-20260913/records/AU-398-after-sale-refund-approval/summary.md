# AU-398｜售后退款审批闭环

`20260820120000_after_sale_refund_approval.sql` 为售后提交和审批建立数据库边界：membership/actor 匹配、动作权限、授权证据、经营范围、审批人与申请人分离、行锁、状态转移、幂等和不可变审批审计都在 RPC 内实施。它还撤销并删除了未受控的旧内部退款命令，要求后续退款闭环提供唯一最终命令。

该迁移的授权函数和两个受控 RPC 仍是后续退款/组织授权闭环的基础，归 G0。固定基线已确认 Commerce API 仍调用被撤权/删除的旧售后、退款和对账 RPC；这一跨服务—迁移契约漂移已在 F-0238 与 F-0239 建立证据，不在本审计分支修复。未发现新增 P0–P3；未执行测试、数据库写入、构建或线上检查。
