# AU-318｜Compatibility 首次改密歧义修正

该前向迁移仅替换 AU-317 的首次改密函数体，消除 PL/pgSQL record 变量与 `memberships.target` 的命名歧义。功能语义保持：仅活动成员且仍处于首次改密状态可更新凭据；成功后递增凭据版本、撤销现有会话并留下审计事实。函数仍仅供 service role 调用。

公开首次改密入口先验证来源和临时口令，不会在仍要求改密时建立会话；现有测试覆盖跨站/缺少来源的提前拒绝和必须改密时不创建会话。审计工作树缺少 Vitest 依赖，未执行测试。

未发现新增 P0–P3 问题或删除候选；该文件是已部署前向修正，结论为 G0（迁移兼容职责）。
