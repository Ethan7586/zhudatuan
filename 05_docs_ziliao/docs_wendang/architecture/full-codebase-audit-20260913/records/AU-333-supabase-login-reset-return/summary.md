# AU-333｜Supabase 登录失败清除返回值

`20260724121500_fix_login_reset_return.sql` 是单一的前向兼容迁移：替换原先返回 `void` 的 `api_clear_login_failures`，在删除对应 IP 哈希的登录失败行后返回 `true`，并保持仅 `service_role` 可执行。它没有改变累计/阻断策略本身。

公开登录、微信绑定、二次验证和初始密码变更成功后均以 `callRpc<boolean>` 调用该函数；调用方只等待完成、不依赖布尔值，因此新返回类型同时匹配 REST RPC 消费模式并不改变成功路径的业务分支。登录累计阻断与项目既定规则的冲突仍由 F-0235 覆盖。

未发现新增 P0–P3 或删除候选。未运行迁移或数据库验证，以避免写入数据库。
