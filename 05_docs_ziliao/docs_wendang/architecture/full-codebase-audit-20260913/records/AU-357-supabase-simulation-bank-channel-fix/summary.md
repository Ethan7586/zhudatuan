# AU-357｜Supabase 模拟银行渠道修正

`20260810161000_fix_simulation_bank_channel.sql` 是已应用测试支付模拟的前向约束修正：将底层 `payments.channel` 枚举明确包含 `bank`，使 `bank_mock` 在模拟混合支付中去除 `_mock` 后可作为可记录的银行渠道持久化。

当前模拟输入校验与混合支付 RPC 都接受 `bank_mock`，因此该迁移保留真实测试回放兼容责任，归 G0。F-0236 的并发幂等问题与渠道枚举无关，仍单独记录。

未发现新增 P0–P3 或删除候选。未改变支付约束或执行模拟支付。
