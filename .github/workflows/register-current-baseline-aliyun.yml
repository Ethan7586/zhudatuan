name: Retired - one-time production baseline registration

# HISTORY: the 1.3 baseline migration has completed. This stub cannot run and is
# neither a normal Delivery Control 1.4.3 path nor a disaster-recovery path.
on:
  workflow_dispatch:

jobs:
  retired:
    name: Retired one-time migration
    runs-on: [self-hosted, linux, x64, retired-workflow-never-runs]
    steps:
      - run: exit 1
