# AU-853｜机器规则调用图与标准对齐台账审阅

- 审阅日期：2026-09-15
- 基线：`5a1ce71eebbefaa826368a9e1dc17730f9363bc4`
- 范围：`governance/zdt-rule-rebuild/06-第三批-机器规则调用图与标准对齐总台账.md`，1 个文件、493 行。
- 方法：深入审阅入口定义、物理计数、workflow/check/audit 调用图、数据库/发布/运行声明、规则—损失映射和候选处置条件；将其自述的 `cc8ab201` 快照与固定审计基线的实际文件和 package/workflow 入口复算对照。

## 结论

台账的治理方法本身保守：区分静态入口、动态入口、DECLARED 与 UNKNOWN；明确候选删除必须补齐调用、运行实例和独有覆盖；不把生成对象清单、迁移词法数量或仓库 Caddy 声明写成生产事实。其规则—损失—退出条件结构可保留为历史决策证据，属 G0。

新增 `F-0330`（P3，高置信）：台账虽然标明 2026-09-12 / `cc8ab201` 快照，却将旧统计称为“当前”。固定审计基线已变为 3 条 workflow、52 个 check 和 32 个 audit；其后新增的 `deploy-oss.yml` 是 OSS→ECS 的手动 Console 发布路径，两个新增 fixture 已有根 package 入口。OSS 与统一发布器对同一 Console pointer 的写入竞争风险已有 F-0016，本单元不重复登记。

未发现 P0；没有运行 workflow、发布、数据库或外部控制面动作，也没有修改业务代码、配置、测试、工作流或运行资源。
