// Generated by @shop/navigationgen. Do not edit.

export const ROUTE_CATALOG_HASH = 'b09829c6b6da59cd97705b8e7df536dcad6e7990cd89df31037e905dbf7cc690' as const;
export const ROOT_PATH = '/' as const;
export const ROUTE_BASE = '/' as const;
export const ROUTES = {
  miniapphome: '/',
  miniappbenefits: '/benefits',
  miniappcart: '/cart',
  miniappcheckout: '/checkout',
  miniappmembercode: '/member-code',
  miniappnotifications: '/notifications',
  miniapporders: '/orders',
  miniapporder: '/orders/:orderId',
  miniappaftersale: '/orders/:orderId/aftersales',
  miniapppayment: '/payments/:paymentId/result',
  miniappcatalog: '/products',
  miniappproduct: '/products/:productId',
  miniappprofile: '/profile',
  miniappsecurity: '/profile/security',
  miniappreferral: '/referral',
  miniappsupport: '/support',
  miniappsupportcase: '/support/:caseId',
  miniappvouchers: '/vouchers',
} as const;
export type RouteId = keyof typeof ROUTES;
export type RoutePath = (typeof ROUTES)[RouteId];
export interface RouteParameterMap {
  miniapphome: Readonly<Record<never, never>>;
  miniappbenefits: Readonly<Record<never, never>>;
  miniappcart: Readonly<Record<never, never>>;
  miniappcheckout: Readonly<Record<never, never>>;
  miniappmembercode: Readonly<Record<never, never>>;
  miniappnotifications: Readonly<Record<never, never>>;
  miniapporders: Readonly<Record<never, never>>;
  miniapporder: Readonly<{ orderId: string }>;
  miniappaftersale: Readonly<{ orderId: string }>;
  miniapppayment: Readonly<{ paymentId: string }>;
  miniappcatalog: Readonly<Record<never, never>>;
  miniappproduct: Readonly<{ productId: string }>;
  miniappprofile: Readonly<Record<never, never>>;
  miniappsecurity: Readonly<Record<never, never>>;
  miniappreferral: Readonly<Record<never, never>>;
  miniappsupport: Readonly<Record<never, never>>;
  miniappsupportcase: Readonly<{ caseId: string }>;
  miniappvouchers: Readonly<Record<never, never>>;
}
export type RouteParameters<R extends RouteId> = RouteParameterMap[R];
type RouteArguments<R extends RouteId> = keyof RouteParameters<R> extends never ? readonly [parameters?: never] : readonly [parameters: RouteParameters<R>];
export const ROUTE_PARAMETERS = Object.freeze({
  miniapphome: [],
  miniappbenefits: [],
  miniappcart: [],
  miniappcheckout: [],
  miniappmembercode: [],
  miniappnotifications: [],
  miniapporders: [],
  miniapporder: ['orderId'],
  miniappaftersale: ['orderId'],
  miniapppayment: ['paymentId'],
  miniappcatalog: [],
  miniappproduct: ['productId'],
  miniappprofile: [],
  miniappsecurity: [],
  miniappreferral: [],
  miniappsupport: [],
  miniappsupportcase: ['caseId'],
  miniappvouchers: [],
}) as Readonly<Record<RouteId, readonly string[]>>;
export const ROUTE_REQUIREMENTS = Object.freeze({
  miniapphome: ['MVPMALLDASHBOARD', 'MVPMALLDESIGN'],
  miniappbenefits: ['MVPMALLFINANCE'],
  miniappcart: ['MVPMALLORDER'],
  miniappcheckout: ['MVPMALLORDER'],
  miniappmembercode: ['MVPIDENTITY'],
  miniappnotifications: ['MVPMALLSETTING'],
  miniapporders: ['MVPMALLORDER'],
  miniapporder: ['MVPMALLORDER'],
  miniappaftersale: ['MVPMALLORDER'],
  miniapppayment: ['MVPMALLORDER'],
  miniappcatalog: ['MVPMALLPOOL'],
  miniappproduct: ['MVPMALLPOOL'],
  miniappprofile: ['MVPMALLSETTING'],
  miniappsecurity: ['MVPMALLSETTING'],
  miniappreferral: ['MVPDISTRIBUTION'],
  miniappsupport: ['MVPMALLSUPPORT'],
  miniappsupportcase: ['MVPMALLSUPPORT'],
  miniappvouchers: ['MVPMALLVOUCHER'],
}) as Readonly<Record<RouteId, readonly string[]>>;
export const ROUTE_FEATURES = Object.freeze({
  miniapphome: 'home',
  miniappbenefits: 'benefit',
  miniappcart: 'cart',
  miniappcheckout: 'checkout',
  miniappmembercode: 'membercode',
  miniappnotifications: 'notification',
  miniapporders: 'order',
  miniapporder: 'order',
  miniappaftersale: 'aftersale',
  miniapppayment: 'payment',
  miniappcatalog: 'catalog',
  miniappproduct: 'product',
  miniappprofile: 'account',
  miniappsecurity: 'account',
  miniappreferral: 'referral',
  miniappsupport: 'support',
  miniappsupportcase: 'support',
  miniappvouchers: 'voucher',
}) as Readonly<Record<RouteId, string>>;
export const ROUTE_GROUPS = Object.freeze({
  miniapphome: null,
  miniappbenefits: null,
  miniappcart: null,
  miniappcheckout: null,
  miniappmembercode: null,
  miniappnotifications: null,
  miniapporders: null,
  miniapporder: null,
  miniappaftersale: null,
  miniapppayment: null,
  miniappcatalog: null,
  miniappproduct: null,
  miniappprofile: null,
  miniappsecurity: null,
  miniappreferral: null,
  miniappsupport: null,
  miniappsupportcase: null,
  miniappvouchers: null,
}) as Readonly<Record<RouteId, string | null>>;
export const ROUTE_DEFAULTS = Object.freeze({
  miniapphome: true,
  miniappbenefits: false,
  miniappcart: false,
  miniappcheckout: false,
  miniappmembercode: false,
  miniappnotifications: false,
  miniapporders: false,
  miniapporder: false,
  miniappaftersale: false,
  miniapppayment: false,
  miniappcatalog: false,
  miniappproduct: false,
  miniappprofile: false,
  miniappsecurity: false,
  miniappreferral: false,
  miniappsupport: false,
  miniappsupportcase: false,
  miniappvouchers: false,
}) as Readonly<Record<RouteId, boolean>>;
export type RouteMatch = { [R in RouteId]: Readonly<{ id: R; parameters: RouteParameters<R> }> }[RouteId];
export function fillRouteTemplate(template: string, parameters: Readonly<Record<string, string>>): string {
  const expected = new Set([...template.matchAll(/:([A-Za-z][A-Za-z0-9]*)/g)].map((match) => match[1]));
  for (const name of Object.keys(parameters)) if (!expected.has(name)) throw new Error(`ROUTE_PARAMETER_UNKNOWN:${name}`);
  return template.replace(/:([A-Za-z][A-Za-z0-9]*)/g, (_, name: string) => {
    const value = parameters[name];
    if (typeof value !== 'string' || value.length === 0 || value.length > 255 || hasControlCharacter(value)) throw new Error(`ROUTE_PARAMETER_INVALID:${name}`);
    return encodeURIComponent(value);
  });
}
export function routePath<R extends RouteId>(route: R, ...arguments_: RouteArguments<R>): string {
  return fillRouteTemplate(ROUTES[route], arguments_[0] ?? {});
}
export function resolveRoutePath(route: RouteId, parameters: Readonly<Record<string, string>>): string {
  return fillRouteTemplate(ROUTES[route], parameters);
}
export function matchRoutePath(pathname: string): RouteMatch | undefined {
  if (!pathname.startsWith('/') || pathname.includes('?') || pathname.includes('#') || hasControlCharacter(pathname)) return undefined;
  const actual = routeSegments(pathname);
  for (const [id, template] of Object.entries(ROUTES) as readonly [RouteId, string][]) {
    const expected = routeSegments(template);
    if (actual.length !== expected.length) continue;
    const parameters: Record<string, string> = {};
    let matches = true;
    for (let index = 0; index < expected.length; index += 1) {
      const pattern = expected.at(index);
      const value = actual.at(index);
      if (pattern === undefined || value === undefined) {
        matches = false;
        continue;
      }
      if (!pattern.startsWith(':')) {
        if (pattern !== value) matches = false;
        continue;
      }
      try {
        const decoded = decodeURIComponent(value);
        if (decoded.length === 0 || decoded.length > 255 || hasControlCharacter(decoded)) matches = false;
        else parameters[pattern.slice(1)] = decoded;
      } catch {
        matches = false;
      }
    }
    if (matches) return Object.freeze({ id, parameters: Object.freeze(parameters) }) as RouteMatch;
  }
  return undefined;
}
function routeSegments(value: string): readonly string[] {
  return value === '/' ? [] : value.replace(/\/$/, '').split('/').slice(1);
}
function hasControlCharacter(value: string): boolean {
  return [...value].some((character) => {
    const code = character.charCodeAt(0);
    return code <= 31 || code === 127;
  });
}
