# Auth UI only. It must never contain database, service-role, or signing secrets.
# Console login and employee registration use the canonical identity API.
# Consumer password recovery remains on the compatibility BFF during the transition.
VITE_API_BASE_URL="http://127.0.0.1:3001"
VITE_CLIENT_VERSION="0.0.0"
VITE_AUTH_API_BASE_URL="/api"
VITE_STOREFRONT_ORIGIN="http://127.0.0.1:3000"
VITE_ADMIN_ORIGIN="http://127.0.0.1:4173"
# Optional isolated-public-staging allowlist entries. Production defaults stay
# limited to the canonical zhudatuan.com origins when these are unset.
# Every supplied value must be an exact HTTPS origin without credentials,
# path, query, or fragment.
VITE_AUTH_STAGING_API_ORIGIN=""
VITE_AUTH_STAGING_STOREFRONT_ORIGIN=""
VITE_AUTH_STAGING_ADMIN_ORIGIN=""
# Vite dev-only proxy target. Auth routes are served by the storefront compatibility BFF.
AUTH_COMPAT_API_ORIGIN="http://127.0.0.1:3000"
