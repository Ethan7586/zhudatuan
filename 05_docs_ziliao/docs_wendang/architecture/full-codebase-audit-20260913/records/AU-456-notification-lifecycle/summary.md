# AU-456｜通知投递、成员偏好与公告生命周期

- 主审 `20260821047000_notification_lifecycle.sql`（192 行），并人工反查通知 API、repository、Worker、事件消费者和任务注册；未执行迁移、投递或线上查询。
- 迁移回填 dispatch/attempt 的 scope、成员、渠道与内容，改为 scope 化幂等键和复合模板外键；缺失关键回填数据会中止迁移。模板激活唯一，成员偏好包含授权状态，公告支持范围/成员受众与有效期。
- Repository 按事件、模板、偏好和范围生成 dispatch/job；notification Worker 处理投递状态与 attempt。RLS 让成员只读取自身偏好、端点、投递/回执和符合范围的公告，运营人员依 scope 管理模板与公告；资源范围纳入统一授权解析。
- **G0**：当前 API、事件和 Worker 实际依赖该模型。**GX-0015**：历史投递、成员可见性和隐私边界迁移，禁止删除、改写或单独重放。未发现新增 P0–P3；未验证历史回填、真实投递幂等、成员/运营 RLS 与恢复演练。
