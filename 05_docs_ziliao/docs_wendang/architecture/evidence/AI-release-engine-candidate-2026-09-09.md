# 全项目统一 AI 发布引擎候选验收证据（2026-09-09）

## 判定

- 候选代码与本地隔离验收：通过。
- 生产切流：未执行。
- 正式“生产 95 分”：暂不签发；必须完成一次性 A3 接线和真实生产计时后评分。
- 基线：`origin/zdt-next@0db8b53e`
- 候选分支：`feature/release-engine`
- 运行时候选源：`b05aedf7907d2343b3300123087d602b8c22e02d`（其后仅追加本验收记录）

## 真实本地计时

| 场景 | 计划 | 构建/验证 | 打包 | 结果 |
| --- | ---: | ---: | ---: | --- |
| 无改动 | 31 ms | 0 | 0 | 10 秒门槛内返回 `NONE` |
| A0 商品/运营媒体 | 16 ms | 17 ms | 7 ms | 2 文件，1,187 B；归档 793 B；零应用构建 |
| A1 Storefront | 31 ms | 8,749 ms | 157 ms | 165 文件，6,583,942 B；归档 1,385,538 B |
| A2 identity-api | 194 ms | 8,934 ms | 68 ms | 5 文件，2,699,715 B；归档 499,627 B |
| A1 相同制品复用 | — | — | 41 ms | 哈希不变，未覆盖不可变制品 |

A1 独立产物在本机以生产模式启动，使用真实 `Host: hbbtzn.com` 请求返回 `200`，响应 98,126 B、79 ms；使用 `Host: zhudatuan.com` 同样返回 `200`，响应 98,126 B、15 ms。裸 `127.0.0.1` 请求按既有主机边界返回 `503`。L0/L1 远端健康探针已分别固化对应 Host，测试进程已精确停止。

## 十项场景矩阵

| # | 场景 | 候选证据 | 当前结论 |
| ---: | --- | --- | --- |
| 1 | 无改动 | 计划 31 ms，等级 `NONE` | 通过 |
| 2 | 商品图片 | A0 计划、构建、打包共 40 ms；无应用构建和重启定义 | 本地通过；媒体源站未接线，生产关闭 |
| 3 | H5 图片/CSS | 只命中 `storefront`；独立产物可真实启动并返回 200 | 候选通过 |
| 4 | 单前端组件 | 定向测试、Storefront 类型检查、一次 production build | 候选通过 |
| 5 | 单 API 服务 | 依赖图只命中 `identity-api`；9.196 秒完成本地计划、验证和打包 | 候选通过 |
| 6 | 候选健康失败 | 自动测试证明失败时 `candidate/current` 均不移动 | 通过 |
| 7 | 切流后失败 | 自动测试证明恢复旧代码与依赖指针并重启旧目标 | 沙箱通过；生产演练待批准 |
| 8 | L1 发布不影响 L0 | 双节点独立指针、进程和策略测试 | 沙箱通过；生产 PID 证据待批准 |
| 9 | L0 发布不影响 L1 | 双向隔离测试 | 沙箱通过；生产 PID 证据待批准 |
| 10 | 同目标并发发布 | 真实目录锁只允许一个持有者，冲突返回所有者元数据 | 通过 |

## 门禁结果

- 发布引擎测试：26/26 通过。
- 部署契约检查：通过。
- 所有发布引擎 JavaScript 语法、安装脚本 Shell 语法、两份 JSON 策略：通过。
- 隔离工作区检查：通过，未借用共享脏工作区依赖。
- 版本回收策略：在 Linux Bash 5.2 + GNU find 环境通过，证明活动指针、人工固定版本、运行进程和依赖版本不会被误删；过期临时项可回收；容量不足会拒绝发布。
- Git 差异格式检查：通过。

## 当前生产停止点

一次性接线会新增远端受控代理、各目标独立 `current/previous/runtime` 指针和候选服务定义，属于 A3。当前 `catalog-media`、`auth-web`、`console` 和 `core` 均由双端策略禁止生产激活。未获得 Ethan 明确回复“开始部署”前，不安装代理、不迁移指针、不重启服务、不修改 Caddy/Tunnel/数据库。

## 评分边界

评分表中不依赖真实生产切流的 80 分已经具备候选证据；“实际发布时间达标”20 分必须由生产上传、候选、切流、健康和外部验收的真实计时给出。因此本记录不以本地测试冒充正式 95 分。
