# AU-846｜SFL 身份域后置绑定根治

## 覆盖与方法

深入审阅 `architecture/07-SFL身份域后置绑定根治.md`（64 行），以当前 realm/account/session 结构、`IdentityNodeRegistry` 和受管 PostgreSQL 夹具作定向结构性核对。未运行迁移、数据库回放、身份测试或线上操作。

## 证据边界

文档给出了 Late Realm Binding 的五批目标、迁移/会话/票据隔离规则与“已完成”陈述。当前代码和夹具存在对应结构，但本文未锁定基线 SHA、日期或复跑输出；而 architecture 文档本身也不自动成为现行权威。因此结构存在不能支持“当前已根治”或“生产已完成”的结论。

## 结论

- 保留为 G1/DC-0109：身份跨 realm 隔离的设计和历史验收线索不可删除。
- 未新增 P0—P3；未将结构性搜索、历史文档或测试名称误写为当前运行验证。
- 未改认证代码、数据库迁移、配置或线上状态。
