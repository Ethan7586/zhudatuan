# AU-346｜Supabase 服务端购物车

`20260725090000_server_cart.sql` 建立早期服务端购物车读取、写入和删除 RPC：所有行均按服务端提供的 tenant、商城和用户范围过滤，写入限定有效 SKU/商品与 1–99 数量，并为新增/更新及删除分别记录审计事实。函数只授予 `service_role`，浏览器提交的数据不成为身份范围依据。

当前 Commerce API 的购物车读取和写入已使用后续的资格/库存快照包装器；删除仍调用本迁移的 `api_delete_cart_item`，该函数通过 cart 所属用户与商城限制目标行，不接受 cart 所有者作为客户端参数。没有证据显示其已被撤权或替代，故归 G0（仍有真实运行职责）。

未发现新增 P0–P3 或删除候选。未运行购物车写入/删除或数据库迁移。
