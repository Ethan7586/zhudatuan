# RV-0034｜财务账本与结算生命周期迁移独立复核

- 复核日期：2026-09-15
- 基线：`5a1ce71eebbefaa826368a9e1dc17730f9363bc4`
- 对应候选：GX-0012
- 结论：**维持 GX，禁止删除、改写或单独重放；未发现 P0。**
- 方法：审阅资金事实/权限模型、当前结算和提现Worker入口；未访问数据库、渠道或账务记录。

## 迁移责任

迁移扩展对账、账单行、匹配项、结算、结算调整/拆分、提现、期间关闭、回填和发票事实表，并施加金额、状态、职责分离、版本及唯一性约束。提现明确拥有`uncertain`状态，避免未知渠道结果被自动重复付款。资源scope解析和RLS把shopapp与shopjob访问边界写入数据库。

它同时把高风险财务操作登记为受控API能力，属于资金、税务、审计与恢复的持久化边界。

## 当前运行关系

`SettlementJob`只锁定已批准且无差异的对账，并要求每个匹配项有有效的支付/退款证据和结算资格；不满足即fail-closed。当前结算、提现、对账、发票命令与Worker仍访问这些事实表、scope与状态约束。后续财务完整性迁移进一步强化追加式账本和平衡断言，而非取代其历史责任。

## 裁决与未知项

维持GX。未验证生产journal/entry对账、账户scope、结算/提现幂等、关闭期间、实际RLS、备份恢复或渠道付款状态。不能从源码声称任何真实资金状态已正确迁移或可恢复；任何后续变更必须从最新主线在独立财务/数据库专项中进行。本审计分支未执行数据库、渠道或部署操作。
