name: Retired - predecessor delivery quality

on:
  workflow_dispatch:

jobs:
  retired:
    name: Retired; normal quality runs inside Delivery Control 1.4.3 Prepare
    runs-on: [self-hosted, linux, x64, retired-workflow-never-runs]
    steps:
      - run: exit 1
