# AU-372｜Supabase 支付验收测试账号

`20260813033000_payment_acceptance_test_account.sql` 是条件测试 fixture：仅在 buyer001 测试成员存在时，为其写入模拟福利/餐补余额和 test_fixture 手机验证；buyer002 保持未验证且空余额，覆盖身份保障对支付的拒绝分支。余额写入使用 `greatest`，避免前向重放降低已有测试余额。

该文件不注册生产支付入口、不接入外部支付渠道，也不包含可复用的生产身份凭据。它仍是支付验收及身份保障反例的历史数据前置，归 G0。

未发现新增 P0–P3 或删除候选；未运行支付、测试或数据库写入。
