# 築大團每日問答

## 2026-08-27

### 問：`archives/` 未來在築大團中負責什麼？

答：

- `main/` 是唯一正式 Git、開發、構建與部署根；`archives/` 與它同層，定位為歷史提詞庫與代碼找回庫。
- 未來需要舊版設計、功能、接口或特殊實現時，先在此搜尋，再按來源證據選擇性提取。
- `main/` 始終是唯一核心；歸檔不能直接運行、提交、部署，也不能整包搬入正式代碼。
- `archives/` 位於 `main/.git` 工作樹之外；正式倉只保存經選定、驗證並記錄來源的內容。

詳細規則見 [archives/README.md](../../../archives/README.md)。

### 問：相對 MVP，渠道與分銷要放什麼？會員與權限是否能找回？系統治理台是什麼？

答：

- 「渠道與分銷」分成兩組：MVP04 的分銷網絡，以及 MVP23 的外部渠道接入。
- 分銷網絡包含分銷商、Tenant 綁定、能力與配額、商品池分配、分銷價格、經營摘要；報表與結算明細跳轉既有報表／財務台。
- 渠道接入包含 Provider 連接、測試與啟停、同步任務、Webhook、外部操作回執和受控重放。
- 現有 `/channels` 只有渠道連接、同步和外部操作，尚缺 MVP04 分銷經營部分。
- 舊版「會員與權限」完整工作台已找到，可以恢復 05_docs_ziliao/VI_shijue/UI/UE；但舊 `/api/v1/admin/*` 接口不能原樣使用，必須對接 2026-08-21 後的新 Contract、Scope、Membership、Access Version 與 Step-up 體系。
- Identity、Member、Access Membership、Qualification 是四個相鄰但不同的領域，介面可以聚合，資料與授權模型不能混在一起。
- 「系統治理台」是風險、通知、會話安全、審計追責與平台准入的跨系統控制面；運行監控屬於智慧翼中控台，日常業務仍歸各業務台。
- 現在的「系統治理台」實際只是資格策略頁，名稱與內容不符，後續需要重新對位。

完整決策與邊界記錄見 [zhudatuan.md](./zhudatuan.md)。

### 本次操作記錄（初始問答）

- 已只讀核對 MVP requirements、Contract Operations、後端模組、現有 Console 與歷史會員權限工作台。
- 新增／更新的只有問答 Markdown 記錄。
- 未修改任何前端、後端、資料庫、契約或部署代碼。
- 狀態：等待使用者下一條指令。

### 問：8 月 21 日 API 要不要搬回 main？現在 API 是否完全可用？

答：

- 不搬原碼。21 號 206 個 Canonical Operations 是現行 217 個的嚴格子集，21 號獨有 Operation、Capability、Event、Migration 與 Service Source 均為 0。
- 217 個 Operations 是正式目標合同；21 號保留為工作台覆蓋、業務行為和測試需求參考。
- `01_core_hexin/services/commerce-api` 暫作 Storefront 的隔離相容 BFF，不能直接與 Canonical API 共用 Migration 或只改 Base URL。
- 程式層測試與 MVP Kernel 已恢復通過，但真 PostgreSQL／Supabase、微信、短信、支付、Provider 和瀏覽器 E2E 證據仍未齊全，所以目前不是「完全可用」或「已可生產部署」。
- 已修復會員商城 Scope RLS 回歸，以及 Canonical／Compatibility 兩套 API 的前臺與後臺身份串線風險。

完整矩陣、測試證據與後續權限順序見 [2026-08-27 API 收斂與權限修復日誌](../operations/2026-08-27-api-recovery-log.md)。

### 問：`zhudatuan/` 的根目錄結構完成度如何？決策文件應放哪裡？

答：

- 最終責任固定為：根 `README.md` 只導航；`main/` 是唯一 Git／開發／構建／部署來源；`archives/` 是 Git 之外的歷史提詞庫。
- `zhudatuan.md` 和 `每日問答.md` 的正式版本位於 `main/docs/decisions/`，不在外層根重複保存。
- MVP 工作簿正式副本位於 `main/docs/福利商城功能清单260821.xlsx`；根舊原型及重複文件只歸檔，不刪除。
- 結構、Git 遠端、CI、API 日誌與根目錄清理全部完成後可評 95/100；餘下分數屬真資料庫、外部 Provider 與正式發布證據，不是目錄搬遷問題。

## 2026-08-28

### 本地收斂驗收

- 外層 `/Users/Ethan/Desktop/Projects/zhudatuan` 已只保留 `README.md`、`main/`、`archives/`。
- 正式 Git 位於 `main/.git`；GitHub 頂層直接是 `01_core_hexin/apps/`、`01_core_hexin/services/`、`05_docs_ziliao/docs_wendang/` 等，沒有多一層 `main/`。
- 舊根工作區與外層 Git 全部可恢復地移入 `archives/`；完整 Bundle 已驗證，沒有刪除歷史內容。
- `npm run quality` 全綠：選定制品門禁、165 Migration、MVP Kernel、TypeScript、Unit／Contract／Journey／Security／Performance、三個前端與核心 API 構建，以及 production dependency audit 均通過。
- GitHub CI 已改成目前三 App、兩 Service 的真實 Baseline；提交 `3a5f3a3` 的 [Main Baseline Run 33095749724](https://github.com/Ethan7586/zhudatuan/actions/runs/33095749724) 完整通過。
- 遠端已實際驗證 PostgreSQL 17、Redis 7.4、Repository、Adapter、165 個 Migration、MVP Kernel、全部確定性測試、三個前端、Commerce Bundle 與阿里雲 Alpine 映像。
- 修復了 macOS 產生的 Lock 漏失 Linux Native Binding、舊整合測試漏算 Observability，以及 Dockerfile 漏構建腳本／Runtime 漏 `pg`、`redis` 的問題；新增門禁防止同類回歸。
- 這代表「正式程式層與部署映像基線」可重現，不代表已通過微信、短信、支付、11 個 Provider、雲端 Supabase／RDS 和正式流量驗收；兩條制品鏈仍維持 `releaseEligible=false`。

### 問：目前前後端分離可以打多少分？

答：

- **全平台 74/100；新版營運後臺鏈路 91/100。** 這是工程架構分離度，不是 UI 完成度或生產上線分數。
- Console 已經通過生成 SDK／Canonical Contract 調用獨立 Commerce Service，瀏覽器不直接操作 PostgreSQL，後臺鏈路的前端、API、資料庫邊界很清楚。
- 全平台被消費端與登入端拉低：Storefront Worker 仍直接嵌入 Compatibility API Router，Storefront／Auth 尚未遷入 217-operation Canonical 合同，並存在 Canonical／Compatibility 雙合同與雙資料庫。
- `accounts.zhudatuan.com` 尚未實際接入，完整 Browser E2E 尚未進當前 CI；`check:frontend` 也仍引用未納入正式 `main` 的 Miniapp 路徑。
- 本次實測 `check:artifacts`、`check:boundaries` 通過；`check:frontend` 因缺少 `01_core_hexin/apps/miniapp/miniprogram/page/home/index.wxml` 無法完成，而不是業務頁面報錯。
- 精確結論：**架構已分離，後臺高度完成，消費／身份鏈路仍在過渡；現在不能宣稱全平台完全分離。**

完整五項評分、扣分證據與補齊順序見 [zhudatuan.md](./zhudatuan.md#2026-08-28前後端分離完成度評估)。

### MVP 對齊里程碑：正式確認前後端邊界

**日期：2026-08-28**

今天完成了一次重要的 MVP 架構對齊，正式確認：

- `main/` 是唯一正式工程；本次判定以 `main/` 的真實代碼、合同、資料庫和 CI 證據為準，不再用歷史快照或單純可預覽的 UI 代替正式狀態。
- 新版營運後臺已形成 `Console → Canonical SDK／Contract → Commerce Service → PostgreSQL` 的清晰鏈路，分離度為 **91/100**。
- 全平台同時包含 Storefront、Auth、Compatibility BFF／DB，因此當前整體分離度為 **74/100**，不能把後臺鏈路的完成度直接等同於全平台完成度。
- 從今天起必須嚴格區分四種狀態：**畫面可預覽、前端已接 API、真實資料庫流程通過、生產環境已驗收**；只有最後一項可以稱為正式上線完成。
- Canonical 與 Compatibility 是遷移期的兩條技術軌道，不是長期保留兩套業務真值的最終方案。
- 下一個 MVP 對齊節點以「Storefront／Auth 合同收斂、獨立 API／BFF 制品、統一身份域、真實 Browser E2E、`releaseEligible=true`」為硬證據；缺少其中任何一項，都不能把全平台評為完全分離或生產就緒。

這個里程碑是後續架構、部署和驗收的比較基線。任何影響前後端邊界的改動，都應在本日記補充日期、證據、分數變化與回退方式。

### 項目治理里程碑：`main/` 進入 Owner 批准制

**日期：2026-08-28**

- Owner 已明確決定：`main/` 內任何功能、服務、設計／VI／UI／UE、API／合同、資料庫／Migration、依賴、測試基線及部署配置的新增、刪除、移動、重命名、替換或實質修改，都必須先取得 Owner 批准。
- Owner 直接下達、且目標和動作清楚的實施指令，才構成該明確範圍的批准；討論、評分、建議、審查、盤點與預覽均不構成修改授權。
- 代碼修改、測試部署與生產發布是三個不同的批准範圍；阿里雲、Cloudflare、正式 DNS、正式資料庫、Migration、切流及回滾必須按環境和版本另行批准。
- 本輪獲批並完成的是：更新外層導航 README、正式 `main/README.md` 的技術棧／制品說明和 Owner 批准制度，以及本日誌記錄。
- 本輪未修改任何業務代碼、API、資料庫、阿里雲資源、Cloudflare、DNS 或正式流量；「準備着手部署阿里雲」目前只代表進入部署預檢階段，不代表已批准正式發布。

### 部署範圍確認：先做好築大團主平台

**日期：2026-08-28**

- Owner 確認本輪只聚焦 `zhudatuan.com` 主平台及其已批准子域名。
- `hbbtzn.com` 保持現狀：不遷移、不下線、不改 DNS、不做資料切換，也不納入本輪部署驗收。
- `main/infrastructure/aliyun/delivery.yml` 內的舊 `hbbtzn`、Store、Supplier、Miniapp 條目只作歷史參考，不是築大團本輪的部署輸入。
- 因此 `hbbtzn` 不是築大團發布阻斷；築大團只按 `02_platform_pingtai/config/artifacts.json` 已鎖定的三個前端、兩個服務及 `zhudatuan.com` 域名體系準備部署。
- 本條只確認範圍並更新文檔，未修改任何雲資源、DNS、業務代碼、部署清單或正式流量。

### 項目結構里程碑：hbbtzn 成為子項目

**日期：2026-08-28**

- Owner 批准將 `hbbtzn` 定位為築大團平台下的獨立部署／租戶子項目，正式位置為 `main/projects/hbbtzn/`。
- hbbtzn 沒有另建或複製 App、Service、Package 或 Database；它引用築大團共享制品，但不加入 npm Workspace，也不改動 `02_platform_pingtai/config/artifacts.json`。
- hbbtzn 的域名、資料、密鑰、雲資源、發布、驗收和回滾責任獨立；當前 `releaseEligible=false`、部署開關關閉。
- 本輪只新增子項目 README、機器可讀身份和停用狀態的阿里雲部署画像，未移動舊代碼，未修改 DNS、資料庫、Secret 或正式流量。
- 歷史 Smart Wing 代碼仍只保存在 `archives/`；運行時硬編碼與舊部署清單須在後續獲批後逐項配置化，不能因建立目錄就宣稱已完成獨立部署。

### 主打團展示部署：真實阻斷記錄

**日期：2026-08-28**

- Owner 已批准本次只部署 `zhudatuan.com`、`console.zhudatuan.com` 與 `accounts.zhudatuan.com` 的展示前端；截至本記錄時，部署**尚未完成**。
- 本機沒有阿里雲 CLI／OSS 發布工具、可用的阿里雲工作負載憑據、已選定的 OSS/CDN Bucket，或可運行 Storefront 伺服器輸出的阿里雲服務／ALB 入口。因此無法安全上傳制品或啟動服務。
- `zhudatuan.com` 的 DNS 由 Cloudflare 託管，但目前沒有登入的 Cloudflare Dashboard Session 或可用的最小權限 API Token；無法先備份現有記錄，亦無法安全切換 DNS、TLS 或 `www` 永久跳轉。
- 本機 SSH agent 沒有可用身份，也未提供目標主機；不能用猜測的主機、帳號或舊 Tunnel 代替經批准的阿里雲入口。
- 未修改任何 Cloudflare／DNS／TLS／阿里雲資源，未上傳制品，未啟動任何生產服務，且未對 `hbbtzn` 的域名、資料、流量或部署作出變更。

### 問：財務系統本輪如何從 50 分提升至 85 分？

答：

- Owner 已授權在唯一正式工程 `main/` 內啟動財務 MVP 升級；archive 與舊 Smart Wing 工作樹只讀，不作任何正式修改。
- 第一階段先修混合支付／部分退款的 Tender／Allocation 口徑、外部與內部雙向對帳、逐科目試算平衡及 action-bound proof／RLS／ExpectedVersion 等 P0。
- 第二階段閉合訂單應收、供應商應付、渠道清算、分銷佣金子帳，差異四眼復核、結算凍結快照／付款回執與期間關閉。
- Console 六個頁籤必須使用權威 typed API；Fixture 僅供隔離的本地視覺回歸，不能作為生產完成證據。
- 驗收以真實 DB 行為、跨 Scope RLS、合同、服務整合、Browser E2E、typecheck／lint／build 的精確結果重新計分；在證據完成前不標記發布基線。
- 本輪不提交、不推送、不部署、不改 DNS／雲資源／Provider 配置；里程碑證據持續寫入 [`../operations/2026-08-28-finance-upgrade-log.md`](../operations/2026-08-28-finance-upgrade-log.md)。

### 問：登錄與權限先做哪個？本輪做到什麼？

答：

- Owner 已確認「先登錄，再權限」；本輪只開始 Console 統一登錄，未開始權限工作台改造。
- 保留已批准的 3003 三段式 VI，Console 密碼登錄已改接 Canonical Identity Session，包含 PKCE、一次性 Ticket、API Host-only Cookie、服務端多身份選擇與 Console CSRF。
- 密碼和 Ticket 不進 URL／History／Web Storage；一次性憑據不進冪等記錄；Ticket 與 Session 有原子鎖定及旋轉。
- Auth、Console、Commerce 型別、定向 Unit／Security 測試、兩個 production build 和 Owner VI Hash 門檻已通過；本地瀏覽器目視確認登錄畫面未被換版。
- 真 DB E2E 尚未完成：財務並行任務的 Migration 白名單尚未收旂，安全門檻拒絕建庫；本輪沒有跨界修改或繞過。
- Compatibility 舊帳號憑據遷移、消費註冊／找回合流與生產部署均需下一次 Owner 批准。詳見 [`../operations/2026-08-28-canonical-console-login.md`](../operations/2026-08-28-canonical-console-login.md)。

### 問：財務欄位能不能新增、修改？不同國家或商品稅率變動怎麼管理？

**日期：2026-08-30**

答：可以。本地財務 MVP 已加入受控的欄位定義與稅務規則工作流：

- 欄位可新增、編輯、停用和版本化，可配置顯示名稱、代碼、適用模組、資料型別、必填性、選項、說明與證據來源。
- 適用模組包含發票、結算、對帳、帳本、應收、應付、渠道清算、分銷佣金、提現與期間關閉；例如買方稅號、免稅代碼、付款核准備註、成本中心、清算地區及佣金率。
- 稅率規則可按國家／地區、稅種、商品稅務分類或 HS Code 設定稅率、含稅方式、優先級、有效起訖日與來源；國家稅率變動時新增下一個有效版本，不修改舊期間的權威版本。
- production 正式變更需平台 Owner 權限、Level 3 二次驗證、action-bound proof、ExpectedVersion、冪等及另一位財務復核人批准；同一發起人不能自行批准。
- 欄位與稅率工作流已通過真 PostgreSQL 17 全量 Migration replay、RLS／四眼／冪等／衝突行為測試、Console 單測、Browser E2E、typecheck、lint 及 production build，本地評分為 **90/100**。
- 仍需明確區分：本輪完成的是配置治理；商品下單時的稅率快照與所有業務記錄的通用自定義欄位值儲存尚未跨界接入。
- 本輪未提交、未推送、未部署；三端部署與 VI 仍處於凍結邊界，不能把本地 90 分寫成線上已發布。
