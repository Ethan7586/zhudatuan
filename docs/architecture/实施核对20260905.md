# 实施核对（2026-09-05）

此文件记录继续实施时实际完成的子项和验收证据，不替代《福利商城代码修改清单20260904》和《福利商城理想方案20260904》，不表示整套方案已经完成。已有未提交修改保留，不以文件存在或单元测试通过代替业务端到端验收。

## 本次续作已核对子项

| 清单位置 | 实际变更 | 验证证据 | 状态边界 |
| --- | --- | --- | --- |
| 6.21.2：Export Process 只渲染授权后的快照 | 凭证、发放单、批量动作创建任务时，同事务复制冻结投影；后台移除对这些实时表的分页读取。搜索导出复用原冻结快照。 | `ExportSnapshot.test.ts` 12 项；其中 1,002 行分页、源表变化、创建幂等、跨租户与跨创建人、任务入队失败整体回滚、快照篡改及缺行、过期和分块清理。 | 这一子项已实现；百万行性能尚未实测。 |
| 6.21.1：导出安全 | 凭证快照仅存密文，业务状态导出不复制密钥；同一创建入口负责 Runtime 任务和冻结快照。所有导出核对最终行数，复用逐页授权复核、CSV 注入防护和逐行水印。 | `SearchSnapshot.test.ts`、`ExportSnapshot.test.ts`、`ExportDownload.test.ts`。 | 对象存储真实联调未验收；一次性签发 URL 不等于 URL 本身只能访问一次。 |
| 6.21.1：凭证导出双人校验 | 复用 `OperationExecutor → PgMakerCheckerGuard` 的现有 ActionProof 消费链，未新增重复审批工作流。 | `CredentialApproval.test.ts` 6 项：操作/请求 Hash/版本/Scope/申请身份绑定、无证明、非法或重放证明、自审批拒绝、已完成幂等重放。 | 此测试覆盖真实执行器和 Guard；同账号多成员身份边界见下方新增 292 子项，完整 UI/鉴权链仍待验收。 |
| 6.21.1：库存申请、发放单与 Approval 调用 | `VoucherApprovalService` 使用可信事务的成员身份提交和取消审批；账号主体继续用于业务审计，不再混传给要求成员身份的端口。统一生成快照 Hash。数量写入 `constraints.quantity`，不再将张数伪装成金额分。 | `VoucherApproval.test.ts` 新增 4 项使用真实 `PgApprovalPort`，覆盖两个业务的提交、取消及审批模板关闭；相关 3 文件 26 项通过。 | 供应申请与完整审批 UI 端到端、多人并发决定仍未验收。 |
| 6.21.2、理想方案 17.3：快照存储 | 新迁移 `20260904028600_prepare_voucher_exports.sql`：强制 RLS、不可修改投影、同 Scope 复合外键、固定行数及字段形状校验；后台只能读取或删除过期数据。定时维护按配置分块清理。 | 346 条空库迁移回放通过（171 历史、175 修复）；真实 SQL 权限和清理测试通过。 | 未在用户存量数据库执行。 |
| 配置唯一来源 | `config/capacity.yml` 管理导出分页、解密并发、快照 TTL、下载 TTL；搜索与其它导出共用，容量上限复用 `model.voucherBatch`；生成器校验范围。 | 配置重新生成、Commerce 类型检查、重复检查通过。 | 不表示整仓所有配置问题已解决。 |
| 6.21.1：产品关键字段版本化 | 删除两个仓储各自写版本的逻辑，创建/修订/绑池/启停统一由数据库记录器写完整快照；直接修改、删除、伪造版本被拒绝；只补录可观察的缺失当前版本，不伪造旧版本。迁移记录实际捕获行数、面值合计并逐条比较快照。 | `ProductVersion.test.ts` 6 项真实 SQL；包含有回填与无回填的审计数量验证。 | 历史版本缺口不被掩盖；未执行存量数据库迁移。 |
| 6.21.1：提交冻结、逐项发券重试 | 新发放单提交时在同一事务保存不可变经济条款；审批冻结产品版本、面值、资格、激活方式及有效期，审批金额为实际面值乘数量。后台及自动激活读取原条款，不读取修改后的产品面值/激活规则。过期或关池不再分配。 | `IssueTerms.test.ts` 10 项；包含真实 SQL 发券、部分失败重试、产品修订/停用后原值不变、旧密钥卡不被自动激活、审批失败回滚、强制冻结和权限边界。 | 该测试中审批与财务端口有替身，不是端到端验收；历史条款迁移保留证据安全门。资格及退款全链仍未完成。 |
| 6.21.1：TenderHold TTL、幂等、互斥及回收 | 后台、结账、支付取消、定时回收共用 `VoucherTenderWriter`；统一 Voucher→Hold 加锁顺序和配置 TTL，预占不超过卡券有效期；过期重放拒绝，重复释放不重复变更；接口检查 Hold 版本；核销重放绑定原订单和预占。回收补时间线，不再将过期卡恢复为可用。 | `VoucherTender.test.ts` 13 项真实 SQL；连同发券维护和结算 3 文件 31 项通过。新增 289 迁移：不可变预占条款、Scope/卡券/预占复合外键；349 条空库迁移回放通过。 | 财务端口为测试替身；锁顺序测试不是 PostgreSQL 多连接并发压测。退款、核销事件归属和现场核验重放仍需继续。 |
| 6.21.1：现场核验幂等 | `VoucherPort` 委托同一核销写入器；核验重试先查原收据，不先用当前余额筛掉已核销卡；完全核销、后来过期仍返回原收据；跨卡、跨 Scope、有效 Hold 均有边界检查。 | `VoucherTender.test.ts` 已增至 16 项并通过，新增 3 项真实 SQL 覆盖零余额/过期重放、跨卡和跨 Scope、不能绕过有效 Hold。 | 核验设备、Token、财务的整链端到端未验收；核销事件归属继续实施中。 |
| 6.21.2、6.28：核销事件所有权及报表 | Voucher 在扣减、收据和账务同一事务内写 `voucher.redeemed` v2；Verification 只调用公开端口，不再代发。订单/门店/后台三类核销明确区分，Scope 与真实门店来自 Organization 端口；金额和币种来自核销事实；财务 OwnerEventId 指向同一真实事件。报表从冻结事件计算，不查询交易表补数据。 | `VoucherTender.test.ts` 19 项，含真实 Outbox、事件 FK、金额不符拒绝、财务失败回滚事件、Outbox 失败回滚核销、伪造门店拒绝；报表新增 3 类事件测试；迁移安全门 3 项。 | 财务和 Organization 测试端口仍有替身；真实队列/报表幂等重放全链和跨时区运营验收未完成。 |
| 7.1、理想方案 12.6：事件版本硬切 | 290 迁移注册 v2；旧 Outbox/Inbox 未处理完则中止，绝不删除未完成事件。旧目录行仅留作历史外键证据，标记退役；Outbox/Inbox 禁止写入非活动版本。当前目录每类型唯一；检查加强为版本/Owner/Schema 逐项一致，并拒绝退役版本遗留待办。Readiness 按活动合同计数。 | 350 条空库迁移回放通过（171 历史、179 修复）；版本退役后的定向 22 项通过。 | 保留历史证据不提供旧版处理入口；历史数据重建仍需独立迁移证据。全仓 Release Head/合同校验和发布尚未完成，不可据此上线。 |
| 6.21.1：原核销退款、原付款人及金额守恒 | 291 迁移在核销事务中捕获原持有人绑定；持有人/核销身份不可变，退款从原绑定查付款人，不读当前持有人。应用与 SQL 函数统一先锁 Voucher 再锁 Redemption；退款 Scope 复合外键、延迟约束强制明细合计等于累计退款；无真实收据或余额更新失败不得返回成功。 | `VoucherTender.test.ts` 24 项、`VoucherRedemptionMigration.test.ts` 4 项、`VoucherSettlement.test.ts` 7 项，共 35 项通过；包含退款后换绑仍可幂等重试、不能冒领、部分退款/超额、财务失败回滚、SQL 篡改拒绝、原持有人证据缺失中止。 | 财务端口仍为替身；此子项不代表退款规则扩展、退款事件与全部跨模块闭环已验收。历史核销未自动回填。 |

每个上述子项完成后，重新读取并核对清单 6.21.1/6.21.2；审批调用另核对 6.5；对应理想方案 8.6、17.3、17.4、17.5。LI 功能清单 9.1–9.4 仍为保留能力的验收基线。工作簿“MVP上线功能清单”此前只读核对，没有修改工作簿或缩减范围。

## 本次续作验证记录

- 发券条款完成时 Commerce 全量：261 文件、874 项通过；Contract 65 项通过。随后产品版本迁移审计增加 1 项，与发券条款合计 16 项定向通过，尚不能将其写成 875 项全量通过。
- 348 条空库迁移回放通过（171 历史、177 修复），包括 287 实际回填证据、288 发放条款安全门。空库结果不是存量切换验收。
- Commerce 类型检查通过；`scripts/check/duplicates.mjs`：0 项违规；`scripts/check/transactions.mjs` 通过。
- 预占共用写入器完成后重新读取清单 6.21.1–6.21.2、理想方案 8.6/18.3/18.4，核对 TTL、幂等、版本、消费/释放互斥、回收和事务边界；没有将真实并发压测列为已通过。
- 预占及核验写入重构后，Commerce 全量 262 文件、889 项通过；随后新增 3 项核验重放定向通过。最新调用检查仍为原 6 项失败。
- 核销事件写入及投影完成时 Commerce 全量 263 文件、901 项通过，Contract 65 项通过，重复与事务检查通过；随后事件目录退役/Readiness 修改已定向通过，正在重新跑全量。未将 350 条空库迁移通过写成存量上线通过。
- 事件目录退役/Readiness 修改后 Commerce 全量再次 901 项通过，重复和事务检查通过。退款子项 35 项定向通过后，已再次读取清单 6.21.1/6.21.2/7.1、理想方案 8.6/12.6/12.7，完整回归与 351 条迁移最终版本核对正在进行。
- 退款子项最终验证：Commerce 全量 263 文件、907 项通过，Contract 65 项通过；351 条空库迁移回放通过（171 历史、180 修复）。最近重复/事务检查通过，调用点仍为原 6 项硬失败。未修改运行环境数据库，未执行部署。
- `scripts/check/calls.mjs`：仍失败 6 项，均为 miniapp、store、supplier 的 `NavigationBinding.ts`、`RouteBinding.ts` 没有真实入口路径。未降低检查规则、未用空入口掩盖。
- 前段续作已验证 Contract 65 项、Storefront 77 项、Design 20 项；这些不代表最新所有跨端端到端验收。

## 仍需继续，不得标记整模块完成

### 新增核对：真实账号职责分离（清单 6.3/6.5/6.21.1，理想方案 8.6/11.5/17.4）

- 通用审批在申请时冻结真实账号，内部决策快照读取冻结值，公开 DTO 不泄露账号字段；按任务及真实账号唯一计票。同一账号切换成员身份不能自批或增加票数。修复真实 SQL 双票成功路径的 `version` 歧义。
- 新迁移 292 固化成员身份与账号绑定，拒绝重绑/删除身份以重用 ID；请求与决定核对可信事务 Actor/Membership；审批金额、币种、Scope 和请求账号不可改写。最终证明与批准实例、决定、对象、金额、证据匹配，消费时再次检查复核账号是否有效。
- ActionProof 原有签发/消费函数继续复用；数据库共用职责分离约束保护签发、消费和 Step-up 请求。旧同账号证明不删除，但不能再消费。历史审批实例缺乏冻结账号证据时，以 `APPROVAL_PRINCIPAL_EVIDENCE_REQUIRED` 中止迁移，未伪造回填。
- 验证：Commerce 全量 264 文件 917 项通过（在最后补入历史同账号证明用例前）；最新定向 3 文件 19 项通过。Commerce 类型检查通过；352 条空库迁移回放通过；重复检查 0 项、事务检查通过，调用点仍为三端原 6 项硬失败。
- 完成后已重新读取清单 6.3/6.5/6.21.1、理想方案 8.6/11.5/17.4，职责分离仍由 Access 低层校验与 Approval 领域共同执行，未另建业务审批流程。测试执行真实 PostgreSQL SQL、审批应用与仓储；动作权限来源是受控夹具，不代表完整生产鉴权、多连接并发或 UI 端到端已验收。用户数据库未执行迁移。

### 未完成项

退款事实子项（清单 6.21.1/6.21.2/6.28，理想方案 8.6/11.21/12.6/13.1）已完成本轮核对：

- 核销时同事务冻结门店、订单渠道、组织范围和时区；退款使用这份持久化上下文，不读取今天的组织关系，也不依赖可能归档的 Outbox。
- `voucher.refunded` 从唯一事件定义生成类型和订阅，在余额、收据、财务冲正同一事务写入；财务 OwnerEventId 指向真实退款事件。重放不追加事件、不重复入账；数据库逐项核对退款金额、币种、规则版本、原核销及冻结上下文。
- 报表分别追加核销总额/次数和退款金额/次数，并按币种分维度；不改写历史核销，不把部分退款当成整次核销撤销。真实队列重放、历史投影重建和报表 UI 仍未验收。
- 新迁移 293 保留 `VOUCHER_REDEMPTION_CONTEXT_EVIDENCE_REQUIRED` 历史上下文证据门。353 条空库回放通过；4 文件 50 项定向、Commerce 类型检查、全量 264 文件 925 项通过。重复检查 0 项、事务检查通过；调用点原 6 项仍失败。
- 完成后回读清单 6.21.1/6.21.2、理想方案 11.21/12.6/12.7/13.1。新增运行的 Commerce Handler 合同套件（不同于 Contract 包 65 项）发现的 5 项 Handler 声明/装配失败已在下一个子项修复。

Handler 声明与装配子项（清单 3.1/6.21.2，理想方案 13.1/16 的 SOLID 门禁）：

- 四个搜索/按卡号/Facet/快照 Handler 各自明确提交的业务调用；共同基类只复用准备、收据与最终响应，不再通过通用执行回调隐藏用例。
- 导出 Handler 只在 `VoucherModule` 构造一次；删除 ApplicationFactory 中重复层级的 Handler 构造函数，保留全部授权、二次核验、事务及下载依赖。
- 未修改 Handler 合同规则。Handler 合同 3 文件 1,432 项、Contract 包 7 文件 65 项、Commerce 类型检查通过。定向搜索/导出/审批 4 文件 36 项通过，新增 Facet 和快照行为测试验证 HMAC 在事务外、写入在事务内、最终返回同一收据。
- Handler 子项随后全量 Commerce 264 文件 927 项通过；类型检查、重复检查与事务检查再次通过。
- 完成后重新读取清单 3.1/6.21.2、理想方案 13.1 和 SOLID 门禁；未新增兼容 Handler 或并行配置。调用点检查仍是三端原 6 项失败，未加空入口。

并发失败排空子项（清单 6.21.2/6.32，理想方案 15.9/18.3）：共用 `mapParallel` 在首个失败后停止领取新项，等待已开始任务结束，再返回原始失败，防止调用方先清理对象/标记故障/重试而旧任务还在运行。保持并发上限和结果顺序；不在 Runtime 混入凭证业务规则。新增 5 项测试，连同凭证导入和导出快照共 19 项定向通过；完成后回读上述两份文档。运行中的外部请求仍依赖各调用方原有 Deadline/Abort 实现，此修改不声称新增了强制取消任意 Promise 的能力。

该子项全量复核：Commerce 265 文件 932 项、类型检查、重复检查 0 项和事务检查全部通过。导入的 XLSX/流式读取、分片令牌/租约和中断续跑仍在继续，不能用这一并发函数修复替代导入全链验收。

1. Voucher 282 存量迁移保留 `VOUCHER_CUTOVER_EVIDENCE_REQUIRED` 安全门。旧数据业务映射、旧 AAD 到新 AAD 的 KMS 重包、数量/金额/历史核对没有完成，不能删除安全门或拿空库通过充当存量切换证据。
2. 平台凭证池向集团/商城的授权供给、LI 分配能力尚未闭环；当前精确 Scope 筛选不能充当跨 Scope 分配。
3. 新发放单已冻结产品经济条款；288 存量迁移保留 `VOUCHER_ISSUE_TERMS_EVIDENCE_REQUIRED`。历史审批条款来源、资格执行及退款原权属等链路仍需补齐，不能以当前产品配置猜测历史批准条款。
4. Hold 共用逻辑、核验收据重放、Voucher 自有核销/退款事件、退款锁顺序及原付款人快照已修正。291/293 的历史持有人及上下文证据门不能移除；退款规则扩展、解绑审批、核验设备和真实事件全链仍需继续核对。
5. 凭证导入原文件的安全隔离、真正 XLSX 解析、流式大文件和断点恢复未完成全链验收。
6. 导出显式下载交互、真正一次性访问令牌/下载通道、对象已生成后授权撤销的对象清理仍未完成。
7. 通用审批及 ActionProof 的同账号多成员身份绕过已修复并增加真实 SQL 测试；其它 Owner/角色分配职责矩阵和完整多人 UI/鉴权链仍需审计，不能以不同成员 ID 充当独立账号证明。
8. Console 预览/占位操作、客户界面、Benefit 后续以及六端一致的完整业务旅程仍未全部完成；三端缺入口是已知硬失败。
9. 全仓其它模块、财务跨模块写入、发布证据、真实外部提供方、压测、六端端到端验收仍需逐条继续，不因本文件记录部分通过而自动变为通过。

## 当前继续点：导入恢复链

- 已完成本轮恢复一致性子项：预检从已持久化分片的连续游标继续，不删除或重新加密已接收分片；分片大小、行并发与租约秒数均来自 `capacity.yml`，生成配置拒绝未知键和非法范围。
- 领取返回单调 Fencing Token；逐行业务写入前在同一事务持有有效租约共享锁；完成、释放、错误明细、计数和 Checkpoint 均绑定精确 `scope + owner + sequence + token`。过期租约只允许令牌递增后抢占；陈旧 Worker 不能继续写或结算。
- 确定性校验/领域错误才进入行错误报告；数据库、KMS、Deadline 等暂时性故障停止领取新行、排空已开始工作并释放本分片供重试，不再伪装成“坏数据”。迁移 294 在存在旧运行分片时硬阻断，并禁止分片内容、归属及边界被改写；仅允许已过保留期的终态父任务删除分片。
- 真实 SQL 测试覆盖断点续传、所有者隔离、错误/成功计数守恒、陈旧令牌、释放重领、过期抢占、不可变载荷、迁移排空和终态清理门禁；Voucher 测试覆盖跳过已加密 1,000 行、租约先于业务写、基础设施错误释放。Commerce 全量 266 文件 940 项、Contract 65 项、类型检查、配置检查、重复检查和事务检查通过；空库 354 条迁移回放通过（171 历史、183 修复）。
- 本子项完成后已完整重读两份权威文档（清单 SHA-256 `968d249a8ebee43c9d580e228f1d8071052154b2ee21761c1134f45dc845f1ff`，理想方案 SHA-256 `8929aa3435d4c842405aecd4c6baf46163b25aa0bac6b798de8f44842bc67ec9`），并反查清单 5.5/6.32/11.5/12.4 和理想方案 8.7/16/17.3。该结果只代表恢复/Fencing 子项，不表示统一 Import Kernel 已整体完成。
- `ImportFile`/`ImportObjectService` 目前只认 CSV 并全量读入内存；`HttpObjectStore.read` 还有限制。不得仅将 32/64 MiB 提高到配置中的 1 GiB，就宣称支持流式大文件；XLSX 也尚未实现。
- Runtime Import 技术骨架仍位于 Foundation 旧路径，六类 `ImportProcessor/ImportRegistry/ImportPreview/ImportErrorFile` 尚未按目标目录完成硬切；Catalog/Inventory/Member/Order/Finance 也尚未全部接入，不能为 Voucher 另写私有导入队列。
- 调用检查仍为原有 6 项失败，均是 Miniapp/Store/Supplier 的生成 Navigation/Route Binding 没有真实入口；本子项未新增调用断裂，也未降低规则。
- 发布状态仍不一致：当前生成合同 Hash 为 `557f81a63e6b54e6d5643480d6ae81f772847d0112cf2973b975c8aaaa37f4ca`，最新迁移为 294，但 `packages/config/src/Release.ts` 仍指向 240。必须完成真实发布目录/Head/证据更新；不得跳过 Readiness。尚未修改用户数据库或执行部署。
