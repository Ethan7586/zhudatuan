# Managed by the Zhudatuan isolated full-staging deployment.
# This drop-in intentionally preserves the distro unit's ExecStart and reload
# behavior; it only supplies the hostnames consumed by /etc/caddy/Caddyfile.
[Service]
EnvironmentFile=/opt/zhudatuan-staging-full/shared/full-caddy.env
