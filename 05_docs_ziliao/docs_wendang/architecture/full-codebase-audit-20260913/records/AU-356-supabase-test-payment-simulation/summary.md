# AU-356｜Supabase 测试支付模拟

`20260810160000_test_payment_simulation.sql` 建立受 RLS 与 service-role 限制的测试充值、福利发放、积分、券和混合支付数据面；每项记录都带 `test_simulation=true`，模拟外部渠道仅使用 mock 值，不触达真实支付方。混合支付以订单/账户/券/积分行锁组合写入支付、账本、子订单和模拟审计事实。

本批复核确认既有 F-0236（P2）：三个模拟 RPC 都先查询幂等键、在后续资金或订单行锁后才插入唯一幂等记录，但没有按请求键取得事务锁或等待后重读。并发相同键重放可让第二请求以唯一键冲突结束，而非返回首个响应。该项仍待独立复核；未重复新增问题。

未运行任何模拟资金写入、迁移或测试。测试入口与定向数据库执行方式尚未确认。
