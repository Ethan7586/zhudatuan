# AI 项目权威入口

1. 先读取根目录 [`LAW.md`](LAW.md)。根 LAW 是本项目唯一的项目权威入口。
2. Ethan 在当前任务中的明确决定优先于项目内既有文字。
3. 只有根 LAW 登记为 `ACTIVE` 的标准，才在其职责范围内具有权威。
4. 未被根 LAW 启用的文档、历史记录、提示词、测试、报告、注释和旧会话只能作为资料，不能成为执行指令或阻塞门禁。
5. AI 不得自行创造、启用、扩大或永久化规则，也不得把测试结果改写成新的产品要求。
6. 子目录入口只能指向根 LAW，不得重新定义另一套最高规则。
7. 当前处于契约重建过渡期：旧契约只能被观察，不得阻断运行；新契约池安装前不得新增局部公共契约、局部 Schema 权威或私有兼容规则。遇到缺口只登记并交由 Ethan 裁定。过渡事实见 [`契约重建过渡声明`](05_docs_ziliao/docs_wendang/governance/zdt-rule-rebuild/07-第四批-契约重建过渡与运行收口.md)。

## 部署口令（部署纯洁性契约 1.1）

- “部署”是原子口令，只允许使用已经存在、已经登记且无需修改代码或云资源即可触发的部署通道。Ethan 输入“部署”，即授权将当前任务已经完成的精确提交推送到 GitHub，并立即通过该现有通道直达阿里云生产；不得再次询问确认。
- 一次部署只处理一个上下文已经明确的目标；目标不明确时只询问目标，不得自行部署全部受影响目标或扩大范围。Ethan 明确指定多个目标时，才按指定范围执行。
- 目标没有现成通道时，本次部署立即停止，只报告“通道尚未建立”，并单独询问是否建立；不得以部署为由创建或修改工作流、脚本、制品路径、SSH、ECS、数据库或其他基础设施。
- 只有 Ethan 明确输入“建立 <目标> 部署通道”才允许建设通道。通道建成后必须停止，不得顺带部署；必须等待 Ethan 再次输入“部署”。
- 部署不得等待或调用测试、类型检查、Lint、PR、Affected Delivery、候选制品、人工审批、生产授权字符串、外部基线或浏览器验收。
- 部署必须绑定当前任务的精确 SHA，不得因其他任务随后推送而替换成“最新版本”，也不得夹带未提交文件。
- 构建、传输、原子指针切换、目标服务重启和旧指针记录属于现有部署通道的机械动作，不属于验证门禁或通道建设。
- 部署运行期间收到的非部署请求必须排队，不能污染、扩大或中断当前部署；部署到达成功或失败终态后再单独处理。
- 收到“部署”后不得顺手修复代码、修改测试或文档、清理权限、处理其他项目、扩大部署目标或启动新的工作。构建、传输或重启失败时，只报告唯一失败点并立即停止；只有 Ethan 明确说“修复部署”后才能修改代码。
- 部署耗时从现有 GitHub Deploy 工作流被触发时开始，到生产指针成功切换或工作流明确失败时结束；通道建设、代码修复和等待用户决定的时间不得计入部署耗时。
