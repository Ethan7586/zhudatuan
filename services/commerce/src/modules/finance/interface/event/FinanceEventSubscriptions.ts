import { EVENT_SUBSCRIPTIONS } from '../../../../generated/EventSubscriptions';

export const FinanceEventSubscriptions = Object.freeze([Object.freeze({ handler: 'reconciliation' as const, events: EVENT_SUBSCRIPTIONS.reconciliation })]);
