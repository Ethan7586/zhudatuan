# AU-320｜Compatibility 支付验收测试账号

该迁移是显式测试夹具：仅在预先存在 `member-test-buyer-001` 时补充掩码手机号、测试验证事实和模拟福利/餐补余额；缺少测试会员时只记录 notice 并跳过，不会创建任何账号、外部支付余额或提供方调用。buyer002 仍保持未验证与空钱包，用于身份保障拒绝路径。

夹具余额使用既有福利账户唯一键和 `greatest` 幂等写法；支付模拟本体只在 development/test 路由与运行模式下注册，已由 AU-310 审阅。审计工作树未运行测试。

未发现新增 P0–P3 问题或删除候选；该文件保留可重放验收夹具职责，结论为 G0。
