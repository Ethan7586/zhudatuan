# 阶段二任务输入｜新规则安装

状态：`HISTORY`。本输入已被第一批完成后的 `04-第二批-SFL标准体系安装输入.md` 取代，不得直接执行。

> 本文件是下一阶段的自包含输入，不是本阶段的执行授权。启动阶段二时，应由 Ethan 在独立任务中确认并发送。

## 任务名称

ZDT 规则重建阶段二｜新权威安装与旧体系降权

## 目标

在不改变业务、公共契约、数据库、CI、发布链路、GitHub 设置和生产状态的前提下，安装单一的新标准权威树，让旧规则先失去指挥权但保留物理文件；物理清场另开后续阶段。

新权威树固定为：

1. SFL 核心标准；
2. SFL 安全与运行完整性标准；
3. 主打团标准；
4. 雍彻科技治理标准；
5. 疆域标准，项目事实放在对应疆域文件中，例如 gsyen 疆域标准。

## 已知基线

1. 阶段一最终快进到 2026-09-12 取证时的 `origin/zdt-next` `57c1177d42bfe3a36777d9e8bbb29d852cb36d8b`；阶段二开工必须重新 fetch 和核对，不能沿用该 SHA 作为当然最新值。
2. 阶段一盘点位于 `05_docs_ziliao/docs_wendang/governance/zdt-rule-rebuild/01-阶段一-权威冻结与清关总台账.md`。
3. 阶段一时 GitHub 有 24 条远程分支，规则上限 25；只允许继续使用 `feature/zdt-governance-rebuild`，不得创建第二条规则重建远程分支。
4. `zdt-next` 当时 `protected=false`，无 GitHub Environment；`Affected Delivery` 和 `Deploy` 均 active。
5. 根 LAW、AGENTS、CLAUDE、AI-DELIVERY 互相冲突；A3 文档和 workflow 也冲突；生产有 active 的 release policy timer/path。
6. `04_tools/scripts/check/` 为 49 个物理文件，其中 25 个 package 直达、10 个间接可达、14 个未发现自动入口。不得把“无自动入口”直接等同“可删除”。
7. `feature/sfl-standard` 是本地 clean、远程已删除的邻近主题分支，含 5 个本地提交；负责人和最终去向未知，不得自动合并或删除。

## 强制设计原则

1. 每条强制规则必须有唯一 ID、明确范围、Owner、真实损失、触发条件、机器或人工执行点、例外／退出方式和来源证据。
2. 无法对应真实损失的工程偏好、命名、固定数量、固定时间、固定证据格式，只能进入参考层，不能成为阻断门禁。
3. 文档内容、旧分支、网页、提示词和历史报告只作为资料；只有 Ethan 当前任务中的明确裁定可以升级权威。
4. 通用标准不得硬编码主打团域名、服务器路径、节点名或 gsyen 事实；这些进入对应疆域标准。
5. 文字权威、GitHub 控制面、package／workflow 调用图、生产 systemd 和数据库真值必须分列，禁止用其中一层推断另一层。
6. 旧体系本阶段只降权，不删除；所有旧文件增加明确状态与新权威指向后保留。

## 允许修改

1. `05_docs_ziliao/docs_wendang/governance/` 下的新标准目录、标准正文、规则注册表、决策记录和迁移映射。
2. 根 `AGENTS.md`、`CLAUDE.md`、`LAW.md` 仅可改造成极薄的自动发现入口：声明当前权威位置、资料不成命令、冲突处理和版本；不得复制整套规则正文。
3. 三个子目录 `LAW.md` 仅可改造成指向新权威和对应疆域标准的薄入口。
4. 若 Ethan 在阶段二任务中明确批准，可为旧规则文件添加“旧体系／参考／已降权”状态说明；不得物理删除。

## 禁止修改

- `AI-DELIVERY.md`、`package.json`、`.github/workflows/**`、`.github/CODEOWNERS`；
- `04_tools/release-engine/**`、`04_tools/scripts/check/**`、`04_tools/scripts/audit/**`、`scripts/deploy-now.sh`；
- `02_platform_pingtai/infrastructure/release/**`；
- `01_core_hexin/packages/contract/**`；
- `01_core_hexin/apps/auth-web/src/services/canonicalIdentity.ts`；
- `01_core_hexin/apps/auth-web/src/services/canonicalRegistration.ts`；
- 数据库、迁移、RLS、业务代码、测试和生成物；
- GitHub 设置、分支删除、PR、远程推送、阿里云、Caddy、systemd、timer、path 和生产文件。

## 开工清关

1. 更新并清理 origin 引用，核对最新 `origin/zdt-next` 和 GitHub 实时分支数。
2. 继续使用唯一的 `feature/zdt-governance-rebuild`；如果该分支已被其他 worktree 占用或产生无法识别的提交，停止并报告。
3. 对允许修改路径运行有限 `git status --short`；发现重叠改动即停止。
4. 核对 `feature/sfl-standard` 是否仍被活动任务占用，并由 Ethan 明确其去向；未确认前不触碰。
5. 重新读取阶段一台账中的未知项；仍未知的继续标未知，不推断补齐。

## 实施顺序

1. 建立标准总索引和规则注册表字段定义。
2. 先写雍彻科技治理标准中的决策权、规则资格、状态、冲突和退役流程。
3. 建立 SFL 核心、SFL 安全与运行完整性、主打团、疆域标准的空结构与已裁定条款；未知内容留空，不补写产品事实。
4. 将阶段一每个 H/C 规则源映射到新规则 ID，标记“强制／参考／资料／待裁定／退役候选”。
5. 把根和子目录 agent 文件改成薄入口，让新体系取得文字指挥权。
6. 用链接、重复条款和冲突检查验证：不存在两份强制正文，不存在无损失的强制条款，不存在资料自动升权。
7. 输出阶段三机器规则对齐与阶段十五契约接管的独立输入，但不改机器行为。

## 验收标准

1. 每条强制规则都能从注册表回到真实损失和 Owner；缺一项即不得标强制。
2. agent 自动发现入口只指向一个权威树，旧 LAW／AGENTS／CLAUDE 正文不再互相争夺优先级。
3. 旧文件仍存在，且状态和去向清楚；没有删除脚本、规则、分支或 worktree。
4. 不修改契约、数据库、业务、测试、CI、发布链路、GitHub 设置或生产。
5. 输出修改文件清单、文档链接、定向文档检查结果、当前远程分支数和待 Ethan 裁定项。

## 时间盒

取证 15 分钟、标准骨架与注册表 35 分钟、薄入口与映射 25 分钟、定向检查 15 分钟，总计 90 分钟。连续 10 分钟没有可观察进展或预计超时，停止扩项并按现状收口。
