# Dedicated full-staging public hostnames. Replace every value with a hostname
# that resolves only to the newly approved, isolated staging ECS.
# Do not use an IP address or any existing production hostname.
ZHUDATUAN_STAGING_FULL_ACCOUNTS_HOST=replace-with-dedicated-staging-accounts-host
ZHUDATUAN_STAGING_FULL_CONSOLE_HOST=replace-with-dedicated-staging-console-host
ZHUDATUAN_STAGING_FULL_API_HOST=replace-with-dedicated-staging-api-host
