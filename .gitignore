node_modules/
.wrangler/
.next/
dist/
coverage/
storybook-static/
.codex-temp/
*.tsbuildinfo
.DS_Store
*.log
.env*
!.env.example
!**/.env.example
!services/commerce/.env.jobs.example
secrets.local.json
infrastructure/container/local/.tls/
infrastructure/container/local/data/

.vercel

output/
tmp/

# Private source documents may contain credentials or customer information.
docs/requirements/private/

# Archived deliveries and parallel worktrees are artifacts, not canonical source.
# They must not participate in search, build, format, test or release (P0A05).
pre-contract-code-merge-20260820/
smart-wing-branch-work/
gitpointer.broken.txt

# Local worker runtime secrets. Never commit.
.dev.vars
**/.dev.vars
.dev.vars.*
**/.dev.vars.*

# Local Codex previews and spreadsheet inspection output.
.codex-preview-*.png
.codex-xlsx/

# Payment certificates and private keys belong in Secret Manager, never the workspace.
config/payment/
