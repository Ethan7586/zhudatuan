export const responsivePattern = Object.freeze({
  frame: 'mx-auto max-w-[1280px]',
  gutter: 'px-3 sm:px-5',
  page: 'min-h-[80dvh] bg-[var(--sw-background)] px-3 py-4 sm:px-5',
  productGrid: 'grid grid-cols-[repeat(auto-fit,minmax(min(100%,7rem),1fr))] gap-3 md:grid-cols-3 xl:grid-cols-4',
});
