# AU-146｜Provisioning 兼容入口与覆盖闭合深审

八个 legacy/root paths are one-way exports to the reviewed public/domain/application/interface layers. Public `index.ts` exports hosted-node/mall-owner provisioning ports, domain purchase policy/lifecycle and manifest only. The Mall Provisioning API and main application import canonical interface/application files, while manifest tests assert public export isolation.

All nine remaining Provisioning files are G0 compatibility/public surfaces. Provisioning 27/27 baseline files now have file-level review status. No P0–P3 new findings; no additional behavior test applies.
