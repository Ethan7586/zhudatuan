# AU-726｜覆盖清单完整性校验

- 范围：`10-coverage-manifest.csv` 的文件级键唯一性与审阅状态统计；不审生产代码，不运行服务或测试。
- 方法：按完整仓库相对路径去重，保留清单中的首条（即最新检查点写入的状态和证据），删除同路径的旧基线或较早检查点记录。

## 结果

- 校正前：4,539 行、4,238 个唯一路径，存在 301 条重复记录；重复会把早期“暂未审阅”与后续完成状态同时计数。
- 校正后：4,238 行、4,238 个唯一路径，重复记录为 0；尚未审阅为 513 个唯一文件。此次仅恢复审计清单的一路径一状态语义，未改变任何代码审阅结论。
- 后续规则：更新已有路径必须替换该路径记录，禁止再追加第二条；文件覆盖、代码行数与状态统计均以去重后的清单为准。
