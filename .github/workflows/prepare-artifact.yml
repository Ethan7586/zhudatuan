name: Retired - legacy artifact prepare

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    if: ${{ false }}
    runs-on: [self-hosted, linux, x64, retired-workflow-never-runs]
    steps:
      - run: exit 0
