# 第一批｜权威状态与旧规则去向表

状态：`REFERENCE`。本文件记录 Ethan 当前决定的实施结果和机器事实，不自行成为下级标准。

## 1. 权威切换结果

1. 根 `LAW.md` 已成为唯一项目权威入口，只负责权威总开关。
2. 旧 LAW 正文、旧 AGENTS／CLAUDE 工作纪律已退出自动权威，内容保留在 Git 历史和阶段一台账中。
3. `AGENTS.md`、`CLAUDE.md` 和三个子目录 `LAW.md` 只保留发现根 LAW 的最小入口。
4. 新标准尚未安装，LAW 登记区全部为“待安装／未启用”，没有虚构 `ACTIVE` 标准。
5. 文档降权不改变 GitHub Actions、脚本、数据库约束、测试或生产控制面；这些机器事实必须另行对齐。

## 2. 文档统一状态登记

| 对象 | 位置 | 状态 | 当前性质 |
| --- | --- | --- | --- |
| 轻量总开关 | `LAW.md` | `ACTIVE` | 唯一项目权威入口 |
| AI 发现入口 | `AGENTS.md`、`CLAUDE.md`、三个子目录 `LAW.md` | `ACTIVE` | 只负责指向根 LAW，不定义下级标准 |
| 旧 LAW／旧 AI 工作纪律 | Git 历史及 `01-阶段一-权威冻结与清关总台账.md` | `HISTORY` | 追溯证据，无自动权威 |
| `AI-DELIVERY.md` | 根目录 | `REFERENCE` | 发布政策资料；GitHub／生产是否执行另看机器入口 |
| SFL 1.5、1.6、1.8 等历史版本 | `05_docs_ziliao/docs_wendang/architecture/08-*`、`10-*`、`12-*` | `HISTORY` | 历史标准版本，不阻塞当前任务 |
| SFL 2.2 的 44 项验收文件 | `architecture/15-SFL-2.2-*` | `REFERENCE` | 44 项结果证据，不是现行标准 |
| SFL 2.2 关键写执行合同矩阵 | `architecture/evidence/SFL-2.2-关键写执行合同矩阵-2026-09-12.json` | `REFERENCE` | 取证材料，不创造契约规则 |
| 历史验收报告和 evidence | `architecture/evidence/**` | `HISTORY` | 追溯当时状态；不能证明今天仍成立 |
| 历史提示词 | `docs_wendang/prompts/**`，现有 9 个文件 | `HISTORY` | 旧任务输入，不能成为当前指令 |
| 运维手册 | `docs_wendang/runbooks_yunwei/**`，现有 14 个文件 | `REFERENCE` | 可供事故处理参考，没有自动阻塞权 |
| 架构建议 | `docs_wendang/architecture/**` 中未被 LAW 启用的文档 | `REFERENCE` | 设计资料，不自动升级为标准 |
| 门禁清关台账 | `/Users/Ethan/Desktop/门禁清关台账.md` | `REFERENCE` | 247 行盘点证据；其中命令不是当前执行指令 |
| 旧阶段二输入 | `02-阶段二-新规则安装输入.md` | `HISTORY` | 已被新输入取代 |
| 无调用入口的 hosted-node 夹具 | `04_tools/scripts/check/sfl-hosted-node-provisioning.pg17-fixture.mjs` | `CANDIDATE_DELETE` | 疑似废弃，等待调用图和物理清场确认 |
| 未列明或证据不足的旧资料 | 任意位置 | `UNKNOWN` | 不得执行、删除或据此阻塞 |

`CANDIDATE_DELETE` 不是删除授权。物理清场前仍须确认活动任务、dirty 状态、独有提交、调用图和进程占用。

## 3. 旧规则去向表

| ID | 旧规则或名称 | 来源／类型 | 当前实际执行 | 试图防止的损失 | 能否对应真实损失 | 主要冲突 | 未来归属 | 建议状态 | 后续机器入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | 默认无条件放行、看到门禁就删 | 旧 `LAW.md`／书面 | 旧 AI 入口曾读取；机器未因此自动删除 | 减少无授权流程摩擦 | 部分；无差别删除会制造反向损失 | 数据完整性、认证、CI 与恢复机制 | LAW 默认放行原则 + SFL 安全与运行完整性标准 | `HISTORY` | 机器控制逐项验损，不批量拆 |
| R-002 | 部署等于直接 rsync | 旧 `LAW.md`／书面 | GitHub 和生产当前不按该文字唯一执行 | 减少发布复杂度 | 有效率诉求，但方案不是永久损失规则 | 发布引擎、回滚与制品追溯 | 当前运行政策／疆域标准 | `REFERENCE` | 是，未来单独裁定发布路线 |
| R-003 | 会话不得互传信息 | 旧 `LAW.md`／书面 | 只靠 AI 遵守 | 防止旧上下文冒充 Ethan 指令 | 部分 | 自包含任务委派 | 雍彻科技治理标准 | `REFERENCE` | 否 |
| R-004 | L0—L6 完成等级 | 旧 `LAW.md`／书面 | 报告模板人工使用 | 防止误报完成 | 部分；固定六级无必要性证明 | LAW 禁止永久固定数量 | 雍彻科技治理标准 | `REFERENCE` | 否 |
| R-005 | 凭据直接交付与密码传递 | 旧 AGENTS／CLAUDE／书面 | AI 入口曾读取 | 避免凭据无法使用和 Shell 截断 | 是，有实际登录失败和时间损失 | 无 | SFL 安全与运行完整性标准 | `REFERENCE`，待安装 | 可能需要凭据操作验收，不新增门禁 |
| R-006 | 密码永久、字符集、无锁定 | 旧 AGENTS／CLAUDE／书面 | 部分由业务代码／数据库实现，具体未知 | 避免 Owner 被锁和密码截断 | 部分；字符偏好不全是强制损失 | 安全控制与业务可用性 | SFL 安全与运行完整性标准 | `REFERENCE`，逐条验损 | 是，需先盘点真实认证机制 |
| R-007 | 安全约束只能 Ethan 添加 | 旧 AGENTS／CLAUDE／书面 | 旧 AI 入口曾读取 | 防止 AI 自设门禁 | 是 | “看到就删”会破坏真实完整性 | LAW + 雍彻科技治理标准 | 已由轻量 LAW 重述 | 否 |
| R-008 | 共享生产主机不得影响其他站点 | 旧 AGENTS／书面 + 阿里云 | 依赖发布方式、Caddy 与人工纪律 | 防止其他项目停机 | 是 | 具体 caddy diff 是否永久必需 | SFL 安全与运行完整性标准 + 主打团疆域标准 | `REFERENCE`，待安装 | 是，需按停机损失直接验收 |
| R-009 | 不伪造 migration ledger、不改已登记迁移 | 旧 AGENTS／数据库 | 迁移链和审计脚本部分执行 | 防止数据库历史失真和重放失败 | 是 | 无差别拆迁移门禁 | SFL 安全与运行完整性标准 | `REFERENCE`，待安装 | 是，数据库真值另查 |
| R-010 | 不硬编码 Owner、不关闭手机验证、不改邀请码 | 旧 AGENTS／业务要求 | 业务代码、测试和生产状态各自独立 | 防止越权、登录失效和邀请模型变化 | 是 | 旧“默认放行”扩大解释 | SFL 安全与运行完整性标准 + 主打团标准 | `REFERENCE`，待安装 | 是，后续按结果验收 |
| R-011 | 单工作区单写入者 | 旧 AGENTS／治理 | Codex 任务依赖人工遵守 | 防止覆盖其他任务改动 | 是 | 无 | 雍彻科技治理标准 | `REFERENCE`，待安装 | 可只保留轻量占用检查 |
| R-012 | 固定时间盒和十分钟停止 | 旧 AGENTS／治理 | AI 人工遵守 | 控制无边界耗时 | 部分；固定数字无独立损失证明 | LAW 禁止永久时间数字 | 雍彻科技治理标准 | `REFERENCE` | 否 |
| R-013 | 20／25 分支上限与 1／2 活动分支 | 旧 AGENTS／治理 | GitHub 未强制；只靠 AI | 降低分支清理成本 | 数量本身不能对应业务损失 | Ethan 已明确不得阻塞重建 | 雍彻科技治理参考项 | `CANDIDATE_DELETE` | 否；仅记录实时数量 |
| R-014 | 分支命名和默认 squash | 旧 AGENTS／治理 | 无 hook；人工遵守 | 提高可读性 | 未证明为真实业务损失 | LAW 禁止命名成为永久条款 | 雍彻科技治理参考项 | `REFERENCE` | 否 |
| R-015 | 禁止强推、误删、未知提交清场 | 旧 AGENTS／Git 治理 | GitHub 未保护；依赖人工 | 防止不可恢复的代码丢失 | 是 | 分支未保护 | 雍彻科技治理标准 | `REFERENCE`，待安装 | 可能；需 Ethan 明确决定是否启用保护 |
| R-016 | 旧系统只读、不得整树并入新轴 | 旧 AGENTS／迁移治理 | AI 人工遵守 | 防止旧架构污染和遗漏独有实现 | 是 | 历史分支去向未知 | 雍彻科技治理标准 | `REFERENCE`，待安装 | 否 |
| R-017 | `zdt.md` 未来权威 | 旧 AGENTS／书面 | 文件不存在 | 建立产品架构基线 | 目标合理，路径未成立 | 新 LAW 已成为唯一入口 | 主打团标准 + SFL 核心标准 | `HISTORY` | 否 |
| R-018 | 固定六项回报和中文版本格式 | 旧 AGENTS／报告 | AI 人工遵守 | 提高人类识别 | 部分；固定格式不是业务损失 | LAW 禁止固定格式永久化 | 雍彻科技治理参考项 | `REFERENCE` | 否 |
| R-019 | 闸门直接测量、HTTP 与 diff 判据 | 旧 AGENTS／测试规则 | 某些脚本和人工验收使用 | 防止错误判定和错误回滚 | 是，有三次错误回滚记录 | 测试不能创造产品规则 | SFL 安全与运行完整性标准 | `REFERENCE`，待安装 | 是，逐个闸门确认直接测量对象 |
| R-020 | 统一 AI 发布入口与 A0—A3 | `AI-DELIVERY.md`／发布政策 | Actions 和发布引擎实际执行部分内容 | 制品追溯、回滚、目标隔离 | 部分；A0—A3 分类不是永久法律 | 旧 rsync LAW；A3 workflow 跳过 | 当前运行政策 + 疆域标准 | `REFERENCE` | 是，后续对齐 workflow 与生产 |
| R-021 | CODEOWNERS 要求 Ethan 负责保护文件 | `.github/CODEOWNERS`／GitHub | 文件可解析；`zdt-next protected=false` | 防止敏感文件无人负责 | 是 | 注释称强制，GitHub 未强制 | 雍彻科技治理标准 | `REFERENCE` | 可能，须 Ethan 决定是否启用保护 |
| R-022 | 历史 SFL 版本 | 架构文档／历史副本 | 无 LAW 启用；代码可能实现其中部分 | 节点、身份和运行一致性 | 逐条不同 | 多版本并存 | SFL 核心标准资料 | `HISTORY` | 第三批再映射实现 |
| R-023 | SFL 2.2 44 项验收 | 验收报告／业务测试 | 报告不执行；对应测试可能独立运行 | 证明节点和关键业务结果 | 逐项不同 | 报告不能创建标准 | SFL 核心／安全／主打团资料 | `REFERENCE` | 是，第三批重建测试调用图 |
| R-024 | 历史提示词 | prompts／历史副本 | 不应执行 | 保存任务上下文 | 否，文本本身不是损失控制 | 可能冒充 Ethan 指令 | 各标准资料库 | `HISTORY` | 否 |
| R-025 | 运维手册 | runbooks／书面 | 人工按需使用 | 故障恢复 | 部分，有真实停机损失 | 具体命令可能过期 | SFL 安全与运行完整性标准资料 | `REFERENCE` | 是，后续与现网核对 |
| R-026 | 架构建议 | architecture／书面 | 不自动执行 | 降低设计错误 | 逐条未知 | 可能与现状或新标准冲突 | 对应标准资料库 | `REFERENCE` | 未知 |
| R-027 | 门禁清关台账 | Desktop 文件／历史盘点 | 不自动执行 | 识别冗余门禁 | 部分 | 统计不是删除授权 | 雍彻科技治理资料 | `REFERENCE` | 第三批复核，不直接照抄 |
| R-028 | package/check/audit 测试规则 | 脚本／业务测试 | package 可调用；Actions 只调用规划选中的子集 | 防回归、数据和业务错误 | 逐项不同 | 文件存在不等于入口 active | 各对应标准 | `UNKNOWN` 到逐项验损 | 是，第三批重新生成调用图 |
| R-029 | 数据库事务、唯一约束、RLS 和已执行迁移 | 数据库真实约束 | 仓库有定义；生产应用状态本批未查 | 防串线、重复扣款、重复订单和数据破坏 | 是 | “清理门禁”不得误拆 | SFL 安全与运行完整性标准 + 主打团标准 | 本批 `ACTIVE` 保护结果 | 后续只在专门数据库批次调整 |
| R-030 | Realm、Membership、Session、登录票据、公司克隆、交易财务结果 | 业务代码／测试／生产 | 各层独立执行，本批未改 | 防身份串线、登录失败、重复公司和交易错误 | 是 | 旧规则降权不等于业务机制降权 | SFL 核心／安全／主打团标准 | 本批 `ACTIVE` 保护结果 | 后续以结果为准重建标准 |
| R-031 | 生产制品来源追溯和停机恢复 | 发布引擎／生产运行 | Actions、回滚目录、systemd 部分实际执行 | 防未知制品和停机不可恢复 | 是 | 具体工具不得永久化 | SFL 安全与运行完整性标准 + 疆域标准 | 本批 `ACTIVE` 保护结果 | 是，保结果不承诺永久脚本 |

## 4. 49 个 check 文件的最新统计

统计基线：本批开工后重新 fetch 的 `origin/zdt-next`。分类口径相互独立时明确标注重叠。

| 类别 | 数量 | 说明 |
| --- | ---: | --- |
| 物理文件 | 49 | 本批及第三批唯一固定基线 |
| 顶层文件 | 42 | `04_tools/scripts/check/*` |
| callgraph 子模块 | 7 | `database`、`events`、`extensions`、`jobs`、`operations`、`routes`、`catalog` |
| package 直接入口 | 25 | 有明确 `npm run` 路径；不等于 Actions 自动执行 |
| 从直接入口递归可达的间接模块 | 10 | 2 个 audit 包装、`source.mjs`、7 个 callgraph 子模块 |
| 无活动自动入口的文件链 | 14 | 11 个独立／手工工具、2 个孤立链辅助文件、1 个疑似废弃夹具 |
| 明确测试／PG fixture 文件 | 9 | 与入口分类重叠；其中 8 个 package 直达，hosted-node 无入口 |
| 疑似废弃 | 1 | `sfl-hosted-node-provisioning.pg17-fixture.mjs`；不得直接删除 |

`check:calls` 可到达 `database`、`events`、`extensions`、`jobs`、`operations`、`routes`，这些模块再到达 `catalog`。第三批必须从 49 个物理文件重新生成调用图，不能沿用文件名或旧台账判断生死。

## 5. GitHub、本地与阿里云规则位置

| 控制面 | 位置／入口 | 当前事实 | 文档降权后的状态 |
| --- | --- | --- | --- |
| GitHub 主轴 | `Ethan7586/zhudatuan` 的 `zdt-next` | 本批查询时为 `5eefb41a…`，`protected=false` | 不受 LAW 文档自动改变 |
| GitHub 分支 | origin heads | 24 条，仅作事实 | 不作为重建阻塞阈值 |
| GitHub CI | `.github/workflows/quality.yml` | `Affected Delivery` active | 机器规则继续执行，本批不改 |
| GitHub 部署 | `.github/workflows/deploy.yml` | `Deploy` active | 机器规则继续执行，本批不改 |
| CODEOWNERS | `.github/CODEOWNERS` | 非空，但无分支保护强制 | `REFERENCE` 责任映射 |
| 本地 hooks | Git 公共 hooks 目录 | 未设置 `core.hooksPath`，无非 sample hook | 无本地自动规则 |
| 当前治理工作区 | `/Users/Ethan/.codex/worktrees/59f0/zdt-next` | 本任务独占目标文件 | 允许本批写入 |
| 活动业务任务 | `/Users/Ethan/Desktop/zdt-next` 两个 active；`afcf/zdt-next` 一个 active | 三处目标规则文件均无未提交改动 | 不移动、不删除、不覆盖 |
| 手工 SFL worktree | `.codex/manual-worktrees/zdt-next-sfl-standard` | clean，远程分支已删除，是否仍占用未知 | 保持 `UNKNOWN`，不处理 |
| 阿里云发布收集器 | `/usr/local/sbin/zhudatuan-release-policy` | 实际存在 | 机器行为不变 |
| 阿里云定时／路径入口 | `zhudatuan-release-policy.timer`、`.path` | enabled、active/waiting | 机器行为不变 |
| 阿里云服务与代理 | `/etc/systemd/system/sfl-*`、`zhudatuan-*`、`/etc/caddy/Caddyfile` | 生产运行事实 | 本批不读取业务数据、不修改 |
| 数据库约束 | migrations、生产数据库 | 仓库约束可见；生产应用状态未知 | 不推断、不修改 |

## 6. 当前冲突

1. 旧 LAW 的“看到门禁就删”与本批明确保护的身份、交易、数据库和恢复结果冲突；旧条款已降为历史。
2. `AI-DELIVERY.md` 描述完整 A3 流程，但当前 workflow 已删除 A3 job 并在候选 job 排除 A3；文字与机器不一致。
3. CODEOWNERS 注释声称 Ethan 批准，但 GitHub 主轴未保护；责任归属与机器强制不一致。
4. 多个历史 SFL 版本和 44 项验收矩阵并存，但 LAW 尚未启用任何 SFL 标准；它们只能作为资料。
5. 本地多个 worktree 保存不同版本规则；本批只修改当前独立工作区，不移动或删除活动 worktree 文件。
6. 前端手写响应 Schema 与后端独立演进形成双事实源的风险已由 Ethan 裁定为后续统一契约重建目标；本批职责边界禁止改契约，不等于永久禁止契约。

## 7. 后续批次锁定与禁止交叉修改

| 批次／任务 | 锁定对象 | 禁止交叉行为 |
| --- | --- | --- |
| 第二批标准安装 | 业务、契约、数据库、CI、发布和生产 | 只安装标准文本与 LAW 登记，不改机器行为 |
| 第三批机器规则清关 | 以 49 个 check 物理文件为固定盘点基线 | 必须重建直接、递归间接、孤立链、手工工具、业务测试和疑似废弃调用图；不得按旧分类删除 |
| 第十三、十四批业务实现 | `01_core_hexin/packages/contract/**`、两个 canonical 文件、公共请求／响应 Schema、根规则、CI、发布文件 | 保持统一契约冻结，不创建新的双事实源 |
| 后续统一契约批次 | 前端 Schema、后端操作目录、contract generator 和公共契约源 | 建立单一契约源，消灭前后端独立演进；开始前另取 Ethan 授权 |
| 物理清场 | 文档、脚本、分支、worktree、服务器文件 | 删除前确认任务状态、dirty 状态、独有提交、调用图和进程占用 |

## 8. 未判断项目

1. 私有仓库套餐下 branch protection／ruleset 详情 API 返回 403；已知主轴端点明确为未保护。
2. 生产数据库实际应用的迁移、RLS、唯一约束和事务清单未查询。
3. 阿里云 RAM、安全组、RDS、OSS、DNS 和证书控制面未盘点。
4. 14 个无活动自动入口文件的人工使用频率和负责人未知。
5. `feature/sfl-standard` 是否仍由活动任务占用、其 5 个独有提交最终去向未知。
6. 五套新标准的完整条款尚未安装；SFL 运行注册表只允许记录可变事实，不得成为第六套标准。
