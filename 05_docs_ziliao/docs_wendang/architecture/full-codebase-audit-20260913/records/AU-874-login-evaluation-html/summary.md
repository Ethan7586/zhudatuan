# AU-874｜主打团登录系统评估历史审阅

- 审阅日期：2026-09-15
- 基线：`5a1ce71eebbefaa826368a9e1dc17730f9363bc4`
- 范围：`主打团登录系统评估.html`，1 个 HTML 文件、403 行。
- 方法：深入审阅历史基线、身份链路结论、ACL/Cookie/UI/三轨身份/测试建议与页尾限制；对当前 `LoginPage.tsx`、身份安全 Cookie、凭据策略、授权票据与登录失败写入做入口和关键差异核验。对已由历史审计说明覆盖的同源结论，不重复逐行深读，仅核对其版本、运行入口和风险差异。

## 结论

该静态 HTML 明确固定在 2026-08-29 的 `be37030`/`13781f1` 工作树，并明确排除了其后 ACL 修复。它记录了当时的 Identity 设计、最小权限 SQL 缺口、未使用 `__Host-` Cookie 前缀、登录页复杂度、兼容身份链路和测试盲区；没有当前运行、构建、发布或文档消费者。

当前代码仍有同名 `shop_session`/`shop_csrf` Cookie、scrypt 凭据校验、PKCE 授权票据、登录失败记录和 1327 行 `LoginPage.tsx`，故历史材料提供了仍需独立核验的线索；但旧行号、当时四个 ACL 失败、覆盖率、三轨实现和“全库最大文件”等判断不能直接变成当前 finding、P0 或修复授权。当前 Identity 结论以对应模块的现行审阅和 findings 为准。

文件归 `DC-0122`（G1）。在身份/安全 Owner 完成外部交接、合规保留期和历史结论到现行审计结果的映射前，应保留；不得据此删除、重构、运行其建议或改变 Cookie/权限策略。

未执行测试、构建、部署、数据库、凭据、浏览器请求或外部控制面操作，未修改业务代码、配置、测试、工作流、迁移或运行资源。
