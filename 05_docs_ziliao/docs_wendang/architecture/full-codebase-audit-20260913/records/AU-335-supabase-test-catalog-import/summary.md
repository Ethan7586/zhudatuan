# AU-335｜Supabase 测试目录导入 RPC

`20260725003000_test_catalog_import_rpc.sql` 是早期受限的测试数据导入器：仅接收 1–200 条约定 `abo_` 标识的 JSON 项，幂等写入专用测试供应商、测试商品、零价 SKU 和零库存，并仅向 `service_role` 授权。它不参与普通用户下单入口。

后续 `20260725005000` 对该 RPC 做过修正；canonical inventory 单一来源切换随后撤销权限并删除该函数。切换契约测试明确断言该旧函数在目标数据库不可再解析。因而源文件仍是前向迁移序列和历史数据演进的一部分，归 G0（历史迁移重放责任），不是可独立删除的代码。

未发现新增 P0–P3 或删除候选。未运行导入或清理操作，避免写入/删除数据。
