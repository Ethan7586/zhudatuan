# AU-353｜Supabase 目录治理 RLS

`20260810113000_harden_catalog_governance_rls.sql` 为分类规则、审阅队列、供应商分类映射和 taxonomy 节点启用 RLS，并撤销 `anon`、`authenticated` 的所有直接表权限。目录治理表因此只能经服务端受控 RPC/迁移路径访问，浏览器角色无法直接读取、写入或截断。

这些表仍被目录治理、复校、移动目录和后续后台治理迁移使用；本迁移形成其直接数据访问边界，归 G0。

未发现新增 P0–P3 或删除候选。未改变数据库 RLS 状态或权限。
