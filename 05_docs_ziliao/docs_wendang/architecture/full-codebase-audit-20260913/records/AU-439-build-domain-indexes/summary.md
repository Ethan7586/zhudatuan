# AU-439｜领域查询索引

## 范围与方法

- 主审文件：`02_platform_pingtai/database/supabase/migrations/20260821028000_build_domain_indexes.sql`（35 行）。
- 交叉核对：Commerce 的订单、支付、通知、渠道同步、outbox/inbox 与 job 运行查询，以及固定数据库迁移序列。
- 本批为静态索引语义与调用关系审阅；未执行 `EXPLAIN`、压力测试或线上数据库操作。

## 运行结论

该迁移为全域常见访问路径建立复合/部分索引：成员范围和会话查询、商品/价格和库存预留、结算过期、订单列表、支付/券状态、渠道同步、支持队列、通知投递、报告、风控、审计及 runtime outbox/job/inbox 领取。

运行代码实际按支付状态和过期时间、checkout 到期、通知 `state + available_at`、channel sync state、outbox 未发布事件和 job 状态读取/更新；部分索引与这些领取和过期扫描谓词相符。迁移本身不改变业务语义或数据内容。

## 审计结论

- G0：服务于当前订单、支付、通知、渠道和 runtime 运行查询的索引集合，不是删除候选。
- 未对真实数据分布或执行计划做验证，无法从静态审阅断定单个索引的收益、冗余或写入成本。
- 本批未新增 P0–P3。
