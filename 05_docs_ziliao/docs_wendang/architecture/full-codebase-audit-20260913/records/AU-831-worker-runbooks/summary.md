# AU-831｜Worker 运行手册族

- 覆盖范围：Commerce jobs.ts 注册的 33 个 runbook，合计约 570 行；全部路径存在，均由 job 注册表以明确相对路径关联。
- 审阅方法：支付查询、订单过期、退款、对账、结算、审计归档、运行清理 7 份涉及资金、订单、不可变审计或删除边界的手册深入审阅；其余 26 份使用同构模板，逐文件核对 job ID、触发条件、责任主体和运行入口，不重复全文阅读。
- 结论：所有手册都是人工作业/事故响应契约，均强调业务键幂等、先查询后重试、保留证据与 dead letter；不被 Worker 作为可执行配置读取，不能因“无源码导入”判为闲置。未发现丢失 runbook、错误 job 路径或新增 P0–P3。
- 未运行 Worker、队列、数据库或任何恢复步骤。
