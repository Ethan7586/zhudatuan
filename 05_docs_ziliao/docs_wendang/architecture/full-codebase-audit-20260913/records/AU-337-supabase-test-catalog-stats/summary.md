# AU-337｜Supabase 测试目录统计

`20260725006000_test_catalog_stats_rpc.sql` 提供仅 `service_role` 可调用的早期诊断投影：全量和测试商品/SKU/库存计数，以及当前数据库字节数。它不写业务数据、不注册用户可达路由。

随后统计函数被目录分类迁移扩展；canonical inventory 单一来源切换最终撤销并删除该函数，目标契约测试断言其不存在。该文件仍属于严格顺序的历史迁移及诊断演进，归 G0，不以当前无调用或最终删除函数作为删除源文件依据。

未发现新增 P0–P3 或删除候选。未执行数据库统计或迁移。
